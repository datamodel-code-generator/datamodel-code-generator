# S03 implementation record

Status: all four PRs are published in native GitHub stack #4110 with coverage, typing and performance gates met locally; none has been merged.

Repository: datamodel-code-generator/datamodel-code-generator. Fixed main baseline: `149d933d927ca12f2e0e45fc18f8a4cf772046d7`. S01 and S02 are merged. The planning PR #4098 remains open at `8e6f0d4a8a64b71e5a049ad8d108507b2fe164ca`; contracts are read from its separate checkout. The original worktree's untracked `1.tmp` is preserved.

## Scope and review boundaries

Implement S03 only, in four native-stack PRs as assigned by PR-STACKS:

1. Operations/type uses and actual resolver arguments/results.
2. Reuse, deduplication, collapse and actual type replacement.
3. Field provenance, five-backend facts and structured type projection.
4. Final symbols/artifacts, accepted-attempt freezing and source-lease disposal.

No new server/client public entry points, dependencies or default-path observers are authorized by this stack. All B requirements and G01–10, dry server/client consumers, strict typing and controlled performance comparisons remain completion gates.

## Integration changes since the pinned plan baseline

Current main adds exactly the six merged S01/S02 implementation PRs since `4f96e22e`: observation baselines, parser/store/resolver/copy factories, shared attempt lifecycle, canonical Api references, explicit Api traversal, and contract comparisons. S03 consumes these existing integration points. Legacy source processing remains the comparison oracle; ordinary explicit Api supplies declaration frames and canonical resolver identity.

## Verification

Initial capture boundary only: 16 E2E tests pass, with 100% line and branch coverage for the three new implementation modules (408 statements, 42 branches). The two basic resolver cases match a literal output fixture separately verified against fixed main. Eight additional Api fixtures match ordinary Api model bytes and original resolver/loader/validation/type-getter/render call counts. Strict mypy 2.3.1 and Pyright 1.1.411 pass for the initial four implementation modules; architecture checks pass. Full S03 coverage, negative typing, final binding, lifetime, benchmarks and CI have not passed yet. S02 results are prerequisite evidence, not candidate verification. Raw S03 investigation and measurement output belongs in `/private/tmp/dcg-s03-evidence`.

Deferred pointer processing reloads a source document into a new raw dictionary. SourceDocumentId therefore follows the actual loader document key, while declaration/producer frames preserve the actual raw node used by each call. A dictionary-identity requirement for document registration incorrectly rejected the existing local-roots fixture; the implementation no longer conflates these two identities.

Typing exceptions are local: the existing snooper class decorator erases inherited method signatures for Pyright; individual super-call diagnostics are suppressed while strict mypy checks their actual source signatures. The existing OpenAPI constructor omits supported Mapping input in its annotation, requiring one argument-specific mypy ignore. The internal resolver source type is explicitly defined without importing private facade helpers. Private `_ApiObject` is used inside its owning parser layer. No Any annotations or casts were added.

Api operation/schema frames are observed only by the capture instance's list subclass. Ordinary Api retains its original list, append/pop calls, and traversal body. Observation preserves actual effective parameter/security mappings instead of recomputing them. Borrowed frame/type/reference anchors are cleared on parser disposal; the source lease is separately closed by its consumer.

## Replacement capture checkpoint

The capture parser now selects ContractGenerationStore at construction. Completed global/scoped reference redirects and field/nested-type replacements retain their actual owners and selected references. No capture query refreshes GenerationIndex. Root collapse saves the root's own structural recipe once per reference before removal, separately from consumer-field state. The context record distinguishes successful mutation at entry from successful exit. Actual field/type copies retain both inherited source fields and the actual merge mode; actual request/response variant returns survive the engine's normal cache cleanup. Disposal clears every ledger collection and removes both instance factory cycles.

The cumulative selected suite passes 148 tests with 100% line and branch coverage across all four new/modified capture modules (632 statements, 50 branches). The binding tests account for 39 cases. Five builtins × three fixtures match fixed-main output fixtures and original model-engine call counts, including GenerationStore refreshes and copy helpers. Deferred inherited merges cover all three existing modes. Cross-module output matches the existing S01 golden. Fault injection covers missing root recipes, structural cycles, and exceptions after replacement entry; ordinary engine failures do not latch capture failures. Strict mypy/Pyright and architecture checks pass.

Measurement correction: the existing process-wide DataModelFieldBase._field_imports_cache warms across parses. A same-process ordinary-versus-ordinary control reproduced the initial import-getter count difference. Comparison tests now clear that existing cache before each side, keeping identical cold-cache conditions. The paired counts then match; no engine cache behavior was changed. New output fixtures were generated using fixed main at `/Users/koudai/work/dcg-6/src`, never the candidate capture implementation.

This checkpoint does not complete S03: final operation normalization, field provenance, structured projection, backend emitted facts, actual accepted-batch freezing, dry consumers, full acceptance, benchmarks, and remote CI/review remain outstanding. No public server/client feature is exposed.

## Field provenance checkpoint (in progress)

The first two review boundaries are published as [#4108](https://github.com/datamodel-code-generator/datamodel-code-generator/pull/4108) and [#4109](https://github.com/datamodel-code-generator/datamodel-code-generator/pull/4109). `gh stack submit` created native stack #4110. Its automatic attribution-only PR bodies were cleared before CodeRabbit supplied descriptions; later CodeRabbit body content is preserved. The third branch is registered locally and has no PR yet.

The new attempt-owned origin index connects actual raw/validated nodes, original property keys, booleans, tuples and YAML alias occurrences. Capture-only producers now retain conditional, inherited/deferred, ref-sibling, allOf object, combined branch and direct allOf ref-union materializations. Direct loader resolution frames exclude parent-map, field-body, recursive producer and add_ref resolution work. Combined branches retain observed false decisions and verified materialization identities, preserving original ordinals after filtering. Common compositions copied before filtering retain their own source-local ordinals. None of these observers invokes another resolver, validator, model dump/copy, renderer or annotation getter.

Required-only fields retain actual required-list occurrences rather than fictional property pointers. Typed-extra and dictionary-root fields retain additionalProperties occurrences. Root-value helper inputs, raw outputs and in-place validation mappings are borrowed for later projection; complete multi-source root-child composition still remains to be implemented. Original pattern/TypedDict metadata type returns and discriminator enum/member identities are retained before string conversion. Cleanup releases all added stacks, lists and borrowed anchors. Item-stream array projections now retain their actual producer and source itemSchema location for both the array and generated item reference. Two repeated-use fixtures preserve output/engine calls and verify that no fictitious itemSchema/items source pointer is introduced.

The last completed cumulative selected suite passed 202 tests (81 parser-binding and 12 model-binding cases), including five-backend byte and engine-call comparisons for forward titled parents, filtered unions, a direct allOf reference with both anyOf and oneOf, reference siblings, conflicting object merges, inline root arrays/maps and synthetic required fields. Baseline Python fixtures were generated independently from the fixed main implementation. Latest measured origin/capture-only coverage is 89%, not the completed S03 gate; guard failure coverage, additional builtin cases and freeze coverage remain outstanding.

Finite final Python type value records and a pure recipe projector now pass five-backend E2E comparisons. Independently enumerated values cover constrained types, containers, generated references, nullable unions, type-sensitive literals and msgspec's actual bool-literal lowering. A counterexample with null first in a union exposed a projection-order error; the projector now follows the engine's null placement while preserving explicit ordered unions. No additional engine getter/import/render calls occur during projection. Strict mypy and Pyright pass.

The accepted-artifact token index is under implementation in `model/binding.py`. It reads each body once and matches known own class fields or functional TypedDict entries, without whole-module AST parsing or executing generated code. Five-backend own-field comparisons and two functional TypedDict cases pass against independent fixed-main oracles, including empty keys, quotes, backslashes, Unicode and Required/ReadOnly wrappers. Emitted-default facts distinguish absent, None, UNSET, MISSING, literal values, unexecuted factory expressions, actual field constructor keywords and type qualifiers. Ten additional five-backend/Annotated comparisons cover builtin mutable-default factories, field-level Annotated factories and explicit default:null. The new model-owned NoneDefaultProvenance helper reads final raw field attributes plus value-only producer context and accepted syntax; E2E tests distinguish schema defaults from proven ordinary optional fallback. Full copy/reuse/default-producer context integration remains incomplete. Finite literal freezing is shared with the projector. Skeleton matching, nested Meta layers and backend model facts remain incomplete. No accepted batch or dry consumer is complete. Do not interpret borrowed observations or this partial index as a finished binding product.

Remote CI initially failed because the generated architecture inventory lacked the new parser subclasses. Regenerating it produced two Mermaid inheritance lines. Commit `110cb6c0` fixes the first branch; native `gh stack rebase --no-trunk` propagated it and `gh stack push` updated both PRs. The aggregate `docs/llms-full.txt` needed the same two lines; commit `10222ed8` updates it. PR #4108 now has 80 passing checks and one intentional skipped production-deploy check. PR #4109 exposed one unreachable guard in the abnormal-case test itself; commit `2b0c87a6` selects the actual root by reference identity instead, and all 39 phase-two binding tests now have 100% line/branch coverage including test code (838 statements, 60 branches). Native rebase/push propagated this correction; PR #4109 now has 79 passing checks, one skipped production-deploy check and only CodSpeed failing. No inline review findings remain unaddressed at this checkpoint. Earlier CodSpeed failure is recorded separately from this genuine coverage correction. CodeQL's no-effect warnings on intentional Protocol ellipsis bodies were addressed with strict-Pyright evidence and resolved review threads, without changing runtime protocol behavior.

Performance work now uses a detached fixed-main checkout at `/private/tmp/dcg-generation-platform-s03-baseline`. Both sides have identical ignored generated version metadata. An initial startup run stopped because that generated file was absent, and is not a completed comparison. A complete 17-case A/A control with 30 balanced pairs, seed 4098 and 99% bootstrap intervals found no significant control difference at alpha 0.01. The complete 17-case A/B comparison with 60 balanced pairs found no significant wall/CPU/RSS differences at alpha 0.01; every 99% interval includes zero. A separate 20-repeat ordinary-generation tracemalloc probe retained no additional dcg objects on either side (main current/peak 4,993/507,258 bytes; candidate 5,829/507,907 bytes). These single memory observations do not establish a significant allocation difference or capture-path lifetime correctness. It measures ordinary generation at this partial checkpoint, not complete S03 capture/freeze overhead or final acceptance.

## Contract hashes

- `ACCEPTANCE.md`: `ab8138656f8013a23da95089abe110216270386f405be793c527254fa782276e`
- `ARCHITECTURE.md`: `b5021f0649466928c4ca7e7520536380accc23ba09a00d26a650b5226f8022ad`
- `BACKGROUND.md`: `77cadae0ab8b153e5e8c89e4be7d1d013ab181cac81b3623b5e469aa60470414`
- `CLIENT-REQUIREMENTS.md`: `b8891572c0b224c20286f4d30507aa2c7eb6503e26ec295fc17c78d9972fda08`
- `COVERAGE.md`: `af5941254e33aeb6713aaa74832f95d0f76881cf57c4d6658fb34bbc2be60b71`
- `DECISIONS-BINDING.md`: `ede700ae13a284fdca2928b075f6b0d0432b0f845284238ee9732e5d58cf867d`
- `DECISIONS-CLIENT.md`: `7a73bdb829f33bf6e9a888c3d2f206c2099789f44e5975d5a352feffd31858bd`
- `DECISIONS-ENTRY.md`: `3a49c001d03c2ac4ef9abaa145f665f88b46e6cdfd8a49780c9f9c5598b5a8c0`
- `DECISIONS-FASTAPI.md`: `ceec759e4d6c553858c57d35660a938e94708b94b2aaaa7afd1aa2dff5a2b688`
- `DECISIONS-MODEL-CODECS.md`: `fc4d804065f11c31e27a3174a2436db6b8cdbb7ae9454641110b8e3a144e1678`
- `DECISIONS-PROTOCOLS.md`: `a751d01cbbce08a40cfabaa5fb1f42a29d6b49dc79f8b63b6d30e7034fb330f1`
- `DECISIONS-RUNTIME.md`: `4d3841b1f4359cf384e924c58ff2155981f03e312f86ec94f90c3aed1bad7c66`
- `DECISIONS-TYPING.md`: `e96bcce1ce1d82f21fac898c3e89ecd164961d1397cc2d85cd52c940a7c8c965`
- `FASTAPI-REQUIREMENTS.md`: `e403e5497ae680eba5faa78d8e79935943f8493313518459815bd230ccecb85f`
- `IMPLEMENTATION-PROMPT.md`: `3990a0c8a9d86132b42c77040f08c2d89dc2de7da81ca847514f9190dffa7f61`
- `IMPLEMENTER.md`: `be5062538bc961655cf3bcf0f62b06034b872674b7d78503764d2841535f8a94`
- `PLAN.md`: `e7661f6f6e65eba283bc6cba4f7be7264321ebe0e4c402b6dbe91dd0b779a48e`
- `PR-STACKS.md`: `8a9fca2aa7b4dda07d856b8657be6c957eca1b6da1abd44fe0db16d9fb6dbfa0`
- `RATIONALE.md`: `9f81875b6c5fa3a0946157b05f12ec79b45c39336f4f9f02e7ab15a3349c5cd2`
- `README.md`: `4b0650cb9297ce568990f4f502b22c53229010343af25b819e2f874bba7f9b08`
- `REFERENCES.md`: `840615466e0107c7e50168dea9091359d6cf3f45b30c616a0bbca6c0f399dcf2`

## Handoff

First PR head: `10222ed8` (`generation-platform-binding-uses`); second PR head: `2b0c87a6` (`generation-platform-binding-replacements`). Working branch: `generation-platform-binding-fields`; isolated worktree: `/private/tmp/dcg-generation-platform-s03`. The third branch contains in-progress field/type work; no third PR is published yet. Latest full selected tests: 202 passed; strict mypy/Pyright cover all eight new capture/binding modules; architecture checks pass. A partial coverage measurement before the latest provenance additions covered projector/literal/artifact code at 85% combined with their E2E tests, while origin/capture-only coverage was previously 89%. These are explicitly not completed S03 coverage gates. The latest safety stash from before the native rebase was successfully reapplied and remains available as `b5dc8fcdc98dda36601067fb3c21322a4b42aece`; earlier safety stashes were retained as well.

Next: complete nested Meta facts and structural skeleton validation with real-generation coverage, then finish multi-source root-child origins and five-backend model facts. Add negative-boundary E2E coverage for the remaining projection/index/provenance branches. Final operation normalization, field copies/C3 ownership, final module identities, accepted-attempt freezing, source lease ownership, dry consumers, full coverage/typing/performance and CI/review completion remain mandatory. No subagents are used. Do not start S04 or merge without authorization.


## In-progress artifact matching and review checkpoint

PR #4108 review r4052355752 identified negative document IDs and leaking lookup exceptions in SourceLease. Commit `992dbab8` rejects invalid IDs and translates missing keys, out-of-range indexes, and oversized decimal indexes to BindingCaptureError. Five real-generation external-fixture cases cover these failures. The first branch passed 130 related tests with 100% line/branch coverage for the capture modules and binding test module (592 statements, 46 branches). Native stack rebase/push propagated the change; the second branch is now `abfd09ec`, with 44 binding cases and 100% line/branch coverage (852 statements, 62 branches). The review reply is https://github.com/datamodel-code-generator/datamodel-code-generator/pull/4108#discussion_r4052457994 and the thread is resolved. CI is rerunning; do not reuse the earlier heads' green status for these heads.

The phase-three checkpoint commit was rebased to `f5373b7a` and pushed as a stack branch, without publishing a third PR. Subsequent uncommitted artifact work adds existing Import/BoundPythonType identity cases, twelve malformed functional TypedDict artifacts, unrelated-scope noise, fourteen changed-type rejection cases, and two renderer-normalized msgspec Meta cases. The cumulative selected suite now passes 245 tests. Ruff and strict Pyright pass; complete new coverage and final acceptance remain outstanding. Accepted syntax is matched to the projected builtin/import/reference/container/union/literal/constructor skeleton; emitted Meta records retain normalized values, order and projected node locations. Bound expression coverage, projected Annotated metadata validation, alias-root metadata, full backend facts and final field-policy integration remain incomplete. No full binding product or server/client dry consumer is complete.

The native rebase safety stash `30f518aa1c134b520312fb0c90d6ce8e31d7fa37` was successfully restored and retained. The original user checkout and its untracked `1.tmp` remain untouched. Continue S03-3, closing its required origin/backend/type gates before claiming it complete; S03-4 is still outstanding.

## Backend facts and field ownership checkpoint

The five builtin backends now have finite model-setting and field-fact records. Ten model-setting cases compare unchanged fixed-main bytes and the generated classes' public native metadata in separate test processes. Omitted settings remain distinct from explicit False/None; renderer omissions are preserved. Capture does not call backend getters or execute factories. FieldSlot now retains its actual own-field ordinal and name.

The new value-only field ownership index preserves class C3 ordering and the distinct functional TypedDict parent-first/raw-key policy. Expanded functional entries retain their consumer identity and original declaring slot; actual accepted entry order/types are corroborated against that view. Its 15 real-generation cases pass with 100% line and branch coverage for the implementation and test module (245 statements, 62 branches), including missing/custom/cyclic/inconsistent ownership and invalid declaration inventories. The emitted index also records known msgspec tag nonemission with absent source coordinates; two malformed-artifact cases reject unexpected declarations and missing owner classes. No generated bytes were changed.

The cumulative selected suite passes 273 tests. Strict mypy and Pyright pass for all nine implementation modules. Complete S03 coverage and acceptance are still outstanding; these are checkpoints, not completion evidence.

Direct warm capture measurements are recorded in `/private/tmp/dcg-s03-evidence/capture-cost.json`: five small backend cases used 20 balanced pairs, and the 568 KB OpenAPI fixture used 10 balanced pairs with seed 4098. Every compared output hash matched. The large case measured ordinary/capture wall times of 395.492/494.477 ms, with paired delta 98.728 ms and 99% interval [82.915, 105.604] ms. Separate tracemalloc probes measured peaks of 43,286,174/67,660,211 bytes. Small cases added roughly 0.45–0.53 ms. These are partial capture costs, excluding final freezing and consumers; single memory probes do not establish leak freedom. The user accepted necessary new-target costs and prioritized completing S03 before discretionary optimization, while retaining the requirement to prevent excessive regressions and unnecessary work.

Remote pre-commit.ci removed the lookup suppression on both published branches. Native `gh stack sync --remote origin` incorporated those commits; main remained at 149d933d. Review r4052469626 was addressed in 091866d3 by narrowing the try block to one lookup statement without changing error behavior. The first-branch suite passed 101 tests and 100% coverage (594 statements, 48 branches); Ruff and strict mypy passed. Native rebase/push propagated the fix, and the review was replied to and resolved. Second-branch head is 45f542ae; the phase-three prior checkpoint is now 3a19181d. CI was still running at this checkpoint. Safety stash c305c263242a1c51a0e3f0049804f7bafeb067e4 was restored successfully and retained.

Next: finish projected metadata/complex type corroboration and multi-source root-child provenance, then close phase-three coverage. Accepted-batch freezing, lifetime integration, dry consumers and the fourth PR remain unimplemented. Do not claim full S03 completion or start S04.

## Nested metadata and root-value provenance checkpoint

Commit c87341a0 saved the backend/ownership work. Subsequent changes corroborate projected discriminator metadata at its actual Annotated type node, rejecting changed/missing metadata, wrong callees and wrong argument forms. Generic container projection now preserves collections.abc versus typing imports, and artifact matching handles FrozenSet explicitly. Independent fixed-main fixtures exercise both import policies and a collapsed discriminator inside an array.

Actual root-value helper dictionaries are now paired with the final validated root and child objects. Captured validation/children/nodes events retain multiple original occurrences, tuple positions and additionalItems without recomputing intersections. The actual resolved-and-copied child sequence is checked before linking reference origins; copied child-ref canonicalization is isolated from the owning loader's two resolver events. Omitted forbidden fields are not added to provenance. Five-backend E2E cases compare unchanged fixed-main output and unchanged ordinary engine call counts; Pydantic BaseModel exercises actual merged array/object/tuple/ref children, while the other four backends preserve their ordinary nonmaterializing behavior. No additional loader, resolver, validation or model-copy calls are used.

The cumulative selected suite passes 286 tests, with Ruff and strict mypy/Pyright passing on nine modules. A partial owned coverage run before the last nested-reference addition reported 89% (2886 statements, 938 branches); complete coverage remains a required open gate. Complex bound-expression corroboration, alias-root metadata, final field/default/reuse integration and full accepted-batch assembly are still incomplete.

Published-head CI: #4108 has 80 passing checks plus the intentionally skipped production deploy. #4109's Python 3.12/isort6 job initially failed one existing runtime Literal-order assertion (8896 tests passed); the failed-job rerun passed without changing source or goldens. #4109 now has 79 passing checks, the intentional skip and only CodSpeed failing. There are no unresolved inline review threads at this checkpoint. The new phase-three work has not yet been published as a PR.

## Structured external type checkpoint

BoundPythonType artifact corroboration now follows the retained expression tree for Callable parameter lists/ellipsis, subscripts, type-sensitive literal values, unions, tuples and native model-field annotation paths. Native-field matching checks the actual backend member, field name and argument indexes without importing the external model or evaluating its annotations. Frozen bound expressions remain the producer-owned values. Seventeen fixture variants cover unchanged fixed-main output and corrupted identities, order, literal kinds and native paths. The cumulative selected suite passes 303 tests; Ruff and strict mypy/Pyright pass. Full owned coverage, final freeze integration and complete S03 acceptance remain open.

## Default producer checkpoint

Each field construction now retains its actual default-policy return and resolver producer, including explicit None overrides. Top-level null evidence is captured before field construction, retaining reference IDs rather than copying a type graph. Nullable collection elements do not imply a nullable collection. Final reference policy resolves the retained IDs conservatively, with external/alias uncertainty preserved. The cumulative selected suite passes 304 tests. Strict mypy on the two changed modules and strict Pyright on all nine modules pass, as does Ruff. Full owned coverage and final accepted-batch integration remain open.

The repeated warm capture measurement is saved in `/private/tmp/dcg-s03-evidence/capture-cost-fields.json`, using the same balanced-pair protocol as the previous checkpoint. All output hashes matched. The large fixture measured ordinary/capture medians of 346.708/435.325 ms, paired delta 88.020 ms and 99% interval [83.812, 90.518] ms. Separate peak-memory probes measured 43,285,986/67,893,291 bytes. These measure the entire partial capture path, not the incremental cost of this commit; changes between separate benchmark runs cannot establish an improvement. Final freezing/consumer costs and complete lifetime evidence are still outstanding.

## Copied field sources checkpoint

Completed field-copy edges are resolved in producer order, retaining both inherited/overriding sources through repeated copies and directional variants. Derived-scope default resolution is associated with the actual existing `_apply_inherited_field_default` return and field pair; no resolver is called again. Explicit None remains distinct from absent source defaults. Fifteen cases cover three allOf modes across five backends against independently generated fixed-main bytes and handwritten source/default expectations. A modular case retains field provenance while ignoring type-only copy edges. An abnormal observer-sequence case verifies the failure latch.

The broader selected run passes 1457 tests with two existing skips, including the full parser suite and generation-store architecture guard. That guard initially flagged the new recipe's `children` attribute as a Reference reverse-edge read; the value record now uses the existing DataType spelling `data_types`, preserving the guard without exemptions. Ruff and strict Pyright pass; strict mypy passes on the three affected modules. A preceding partial capture-only coverage run passed 95 tests but failed the required 100% gate at 88.57%; complete owned coverage is still outstanding. Final field-policy assembly, accepted-batch freezing, lifetime proofs and final performance acceptance remain unimplemented/incomplete. Published-head CI remains 80 passes/one intentional skip for #4108 and 79 passes/one intentional skip/CodSpeed failure for #4109.

## Decimal constraint corroboration checkpoint

Actual condecimal kwargs retain exact Decimal values in the type recipe. The accepted-artifact matcher now recognizes the established decimal.Decimal import and one literal string argument, comparing finite Decimal values without executing generated code. Ten real-generation variants include equivalent exponent notation and reject changed values, wrong callees, extra arguments, non-string input, invalid decimals, infinity and signaling NaN. Original output is independently frozen from fixed main. The selected parser/binding/driver/API suite passes 1467 tests with two existing skips; binding tests alone pass 98 cases. Ruff, strict mypy and strict Pyright pass. This closes one valid type-corroboration gap; complete S03 coverage and final accepted-product assembly are still outstanding.

## Third PR and CI checkpoint

PR #4111, "Capture final model field facts", is a draft in native stack #4110, based on generation-platform-binding-replacements. Its initial head was be77186f. Its body is empty; only the CLI-generated attribution footer was removed before any bot-written body existed. The third PR is not complete. A refreshed owned-production/test coverage run passes 222 tests but fails the 100% gate at 91.25%; all three owned test modules and the ledger/contract/lease/ownership modules are at 100%, while projection/origin/artifact modules still need coverage and integration. Evidence: `/private/tmp/dcg-s03-evidence/phase3-current-coverage.json`.

The initial third-PR checks job found five ty 0.0.9 diagnostics: narrowed literal names and optional token applications after a NoReturn method call. Raise sites now explicitly raise the shared error value, and the builtin match binds its literal alternative. Strict mypy, Pyright and ty pass on the changed modules; Ruff passes and 207 affected real-generation tests pass. A whole-source local ty check reports only a pre-existing unused tomli suppression caused by the local environment having tomli installed; no existing suppression was edited. CI also reproduced the existing Literal argument-order instability in three field_name_bindings cases on Python 3.12/blacklatest. No source/golden change was made to hide those failures. CI must be rechecked on the next head. The fourth branch/product freeze is still outstanding.

## CI determinism correction

CodeQL's possible-uninitialized default_kind report is addressed by initializing the conservative opaque result before the finite match. Existing Literal snapshot failures were independently reproduced using only typing: prewarming Union[Literal['Optional', 'list', 'Field'], None] makes the equivalent differently ordered union reuse that first Literal order. The runtime test now orders observed members by the source fixture's enum sequence. Unexpected members still fail, missing members still differ, and all existing generated-byte and runtime expected files are unchanged. All 228 field_name_bindings cases pass both normally and after deliberately prewarming the conflicting typing cache. The 98 binding tests pass. Ruff migrated six existing suppression comments in the touched test module to its required spelling, without adding suppressions. Strict mypy and ty pass for the changed implementation.

## Repository ty upgrade

The user requested keeping the repository on the latest ty. The version update and the type fixes it requires in pre-existing modules are a separate PR, #4141, at the bottom of the native stack. This stack keeps only the S03 modules' own adaptations to the newer checker.

## Third-PR failure lifetime checkpoint

Retaining a latched producer exception must not retain its parser or generated graph after disposal. The ledger now clears tracebacks through the exception cause/context graph without replacing the original exception identity. Independent E2E cases deliberately damage actual producer observations during real generation; ordinary engine methods still run. Cases cover combined-branch correspondence, allOf loader ordering, root-value materialization, reference siblings, and field-default ownership. No normal-path mock or new assertion helper is introduced.

On the third branch, the full Python 3.13 suite passes 19,601 tests with 15 existing skips in 138.64 seconds. The completed producer-failure matrix passes 30 cases, each retaining the first exception and observing zero surviving parsers and graph objects. Warning-strict full-source ty and Ruff pass. Full owned line/branch coverage is still incomplete; these results do not mark S03 complete. The fourth branch already has the accepted-session counterpart of the cleanup correction and must be restacked with the shared fault fixtures.
## Final-module capture checkpoint

Native `gh stack add generation-platform-binding-freeze` created the fourth local branch on top of #4111. No fourth PR exists yet. The capture-only module hook now borrows the original Result object, actual module key, ordered declaring models, and existing import collections after the single ordinary render. It performs no freeze inside parse. Two real-generation cases preserve fixed-main single-body and existing modular outputs, verify Result identity, and prove borrowed model references are released after disposal. The broader selected suite passes 1469 tests with two existing skips; Ruff, strict mypy/Pyright and affected-module ty pass. The immutable accepted batch, address normalization, source-lease transfer and dry consumers remain unfinished.

CI on #4111 head 222e8938 passed the ty checks job, but CodeQL reports default_kind as potentially uninitialized across a statically exhaustive match. Two jobs again hit existing Literal-order snapshots. A generator-independent CPython experiment confirms Union caching reuses equivalent Literal arguments in their previously cached order. The lower-branch correction described above now makes the opaque fallback explicit and stabilizes only test-time Literal observation in original schema enum order, keeping all generated-byte oracles and expected fixtures unchanged. CI must be rerun on that corrected head.

## Actual result address checkpoint

On generation-platform-binding-freeze, after df367003, module result binding now uses the existing normalization/expansion helpers under the ordinary renderer's conditions. It verifies actual Result identity at the primary key, records secondary shared-init keys in actual result order, and returns finite missing/collision diagnostics. The frozen mapping contains model IDs and result keys only. It does not claim to supply logical staged artifact paths or a complete accepted contract batch yet.

Four real parser cases cover single bodies, single-model splits, dotted directories, hyphen normalization and actual shared parent init Results. New generated-file oracles came from detached main 149d933d; address expectations were specified separately. The selected freeze/binding/assert-helper suite passed 131 tests. Strict mypy passed on the two changed capture/contract modules. Strict Pyright passes on all nine modules after restoring an explicit opaque match arm; this retains the initialized fallback needed by CodeQL. Ruff passed on the changed capture/test files. Complete owned coverage and final integration remain open.

The user reaffirmed that normal-path mocks/test doubles are prohibited. The four S03 monkeypatch sites inject capture registration failure, collapse failure/corruption, or duplicate default-resolution records; all are exceptional cases. Profiling observers count real calls and do not replace their implementations. Earlier S01's AttemptConsumer still substitutes a test-only receiver in normal initial/repair cases. Those cases must migrate to the real accepted-batch session and the normal-path substitute must be removed; this migration has not been completed. Do not treat component tests as final end-to-end acceptance.

At this checkpoint PR4111's corrected head passed typing, CodeQL and the previously failing Python 3.12 jobs; three checks were still pending at the last inspection. Its CodeRabbit status explicitly says review skipped because the PR is a draft. No full-green or completed-review claim is made.

Next: assemble actual final symbols/field facts and accepted batches, wire the concrete session through the existing driver, migrate the normal initial/repair consumer tests, then finish missing E2E coverage and final performance/CI/review acceptance. No S04 work or merge is authorized by this checkpoint.

## ty version comparison

The user asked whether old ty inference explained the earlier typing mismatch. CI's recorded environment, uv.lock and both local comparison installations use ty 0.0.9. An isolated installation of the official 0.0.82 release checked the same pre-fix be77186f source, same Python environment and same diagnostic flags. Version 0.0.9 reports the original five narrowing diagnostics; 0.0.82 reports none of those five and instead reports one redundant cast at model/binding.py:1328. Thus the five diagnostics were old-checker inference limitations, not demonstrated runtime bugs. Commit 222e8938 makes the code compatible with the currently locked CI checker. No repository dependency or lockfile was changed. Comparison logs are /private/tmp/dcg-s03-evidence/ty-009-before-fix.log and ty-082-before-fix.log; the isolated source and environment are outside the checkout.

Latest PR4111 CI has 78 passing checks, one intentional deployment skip, and failures in combine coverage, codecov/patch and dependent test-gate. Full-source coverage reports 242 missing statements and 163 partial branches, below the required 100%; this confirms the already documented missing binding acceptance coverage. PR4108 and PR4109 have no unresolved inline review threads at this inspection.

## Imported constraint expression checkpoint

Accepted-annotation corroboration now matches retained ImportedExpression values against the actual import identity and surrounding expression tokens without evaluating calls. A real Decimal-default fixture exercises the parser's constraint normalization into PythonRuntimeExpression; enabling the option without a default did not reach that producer. The new generated-byte expectation was obtained independently from baseline 149d933d via ApiOpenAPIParser. Ten existing corruption cases now run with and without actual default deserialization (20 cases). The broader binding/module-freeze/assert-helper suite passes 141 tests, and full-source warning-strict ty, strict Pyright, Ruff and formatting pass. A post-refactor repeat of all 20 Decimal cases passes. No normal-path mock or new assertion helper was introduced. Final accepted-batch assembly, owned 100% coverage and full S03 acceptance remain incomplete.

## Independent Astra Max review and final-inventory integration

At the user's explicit request, an independent Astra Max reviewer examined cumulative commit 6ff71d3b8921eb619d567d5f9c06847fb88bad37 against base 149d933d927ca12f2e0e45fc18f8a4cf772046d7, including the exercised S01 driver and S02 scope boundaries. The review was read-only and did not alter branches or PRs. It found seven P2 issues in existing components:

1. Inline anyOf/oneOf inside allOf was incorrectly required to have a direct loader event.
2. Final type projection retained producer imports after ordinary import_overrides remapping.
3. Unverified assignments/imports could shadow builtin annotations without invalidating corroboration, including builtin leaves in BoundPythonType.
4. Functional TypedDict fields were incorrectly keyed by normalized Python name rather than consumer entry ordinal and declaring slot.
5. The ordinary empty fixed-tuple spelling Tuple[()] was counted as one type argument.
6. Ordinary quote normalization and Black's parenthesized adjacent literal wrapping changed SourceExpression/ImportedExpression token spelling without changing values.
7. A latched recording exception retained disposed models through direct/chained traceback frames.

These findings now have independent real-generation regression inputs. Original model-output expectations were obtained from fixed baseline 149d933d, while type/key/origin expectations were written from the input and existing output semantics. Corrupted accepted artifacts and recording exceptions are confined to abnormal cases; no normal-path mock or assertion helper was added. The inline-union guard preserves original property pointers. Import remapping uses final stored module namespaces, including BoundPythonType leaves, and has a fast path when no remapping or alias resolution is needed. Unverified bindings remain distinct from absent builtin bindings. Finite literal comparison accepts quote/wrapping changes without evaluating calls. Ledger cleanup preserves the first failure object and removes its traceback chains.

A new private final-model inventory joins actual declaring module outputs, model/reference IDs, own FieldSlots, base identities, final type projections, explicit output/package addresses, and actual module imports. It is an intermediate graph-free inventory, not a completed GeneratedTypeContractBatch or public product. The reviewer also caught an error in the initial uncommitted SerializeAsAny policy: final emitted inheritance does not reconstruct the original reference.children predicate after reuse. The bounded model-side projection now reads the actual final stored relation only when the option is enabled; it does not invoke the type-hint getter. The reduced forward-inheritance/reuse case verifies actual emitted Optional[ForwardBase] without inventing SerializeAsAny.

Independent follow-up evidence: the reviewer passed 13 tuple/TypedDict/traceback regression cases and 20 anyOf/oneOf × five-backend × Api/Schemas ordinary/capture comparisons. A separate reviewer sweep ran 227 local OpenAPI inputs across five backends: 1,079 equal successful outputs and 56 failures on both paths, with zero output differences or one-sided success. This establishes only the measured same-engine capture parity, not fixed-main performance or final-batch acceptance.

The implementation-side selected binding, ownership, module-freeze, capture and assertion-helper suite passes 288 tests. Strict Pyright passes on all eleven capture/binding modules. Warning-strict whole-source ty and both architecture/store guards pass. This checkpoint does not close full owned 100% coverage, accepted-batch/session assembly, dry server/client consumers, retry/discard proofs with real batches, staged-artifact validation, or final performance acceptance. The original S01 normal-path AttemptConsumer replacement remains pending until the concrete session exists. S03-3 is still draft and S03-4 has no PR; S04 has not started.

After the additional bound-builtin shadow and long wrapped-literal cases, the four affected implementation test modules pass 259 tests. Strict mypy and strict Pyright pass on eleven modules; whole-source warning-strict ty, changed-file Ruff, and both architecture/store guards pass. The initial test command incorrectly named a nonexistent standalone type-projection test module and collected no tests; the corrected command includes the actual projection cases in tests/model/test_binding.py.

The capture-cost-inventory.json measurement uses 60 balanced pairs per case with seed 4098, identical candidate-engine settings and independently checked output hashes. Small five-backend paired wall-time deltas are 0.668–0.709 ms. The large case's ordinary/capture medians are 350.661/488.055 ms, paired delta 137.400 ms, 99% interval [136.324, 138.396] ms; separate peak-memory probes are 43,285,845/67,830,818 bytes. All measured final fields project successfully. This includes constructor, parse, final identity/type/import inventory, disposal and lease close, but excludes complete field/operation batch assembly and consumers. It measures selected-feature cost, not ordinary-path nonregression or complete S03 performance acceptance. Raw evidence is outside the repository under /private/tmp/dcg-s03-evidence/.

## Accepted product and directional regression checkpoint

The fourth branch now uses an actual OpenAPIGenerationSession in the existing shared driver. The driver chooses the accepted AttemptId; freeze completes before disposal; rejected attempts and construction/disposal failures release their ledger and source resources. Products contain ordinary artifact bytes, graph-free field/type/operation contracts and a separately owned SourceLease. The nineteen S01 attempt cases now use real capture and unchanged trace oracles; the normal-path AttemptConsumer/AttemptParser test doubles were removed. Fault replacement remains confined to explicitly abnormal cases.

Final field assembly covers the five builtin backends, source occurrences, final declaring slots/imports, actual artifacts, and directional exclusions. Pure demand checks follow required generated symbols and helper-producing fields. Accepted artifact validation detects missing/shadowed declarations, changed field facts, bases, decorators, model settings and imported identities. Validation indexes each module and partitions it once by model so independent good types in the same file remain usable without repeated whole-module scans. Schemas-only and unambiguous local Paths scopes use existing emitted component identities and the shared operation normalizer; no API traversal, schema parsing or extra model registration is added to those scopes.

The requested independent Astra Max reviewer examined cumulative S03 and its exercised S01/S02 integration. Additional real counterexamples covered function/del/unpack/augmented assignment shadowing, imported-base replacement, scalar helper dependencies, builtin container aliases, directional reuse across distinct schemas, inherited field order, and directional array-root provenance. The fixes have independent handwritten factsheets and fixed-main model byte oracles. The reviewer separately repeated 15 five-backend ordinary/capture comparisons with identical bytes and zero retained parser/ledger nodes, and confirmed the reproduced defects were fixed. This is an interim review, not final S03 acceptance.

Current implementation-side checks: 471 selected tests pass (model bindings, ownership, parser capture/freeze, real attempt lifecycle and accepted products); strict Pyright checks 17 owned modules with zero errors; full-source warning-strict ty passes; architecture and generation-store guards pass. Changed-file Ruff/formatting were checked. Eighty directional combinations compare against ordinary baseline 149d933d927ca12f2e0e45fc18f8a4cf772046d7; 15 further five-backend legacy/API scope cases compare ordinary bytes and independent demand outcomes. The baseline all setting intentionally retains Envelope/Array where the ordinary generator does not create directional wrappers.

Remaining gates are explicit: complete nested helper/directional/default/provenance acceptance, legacy semantic exclusion and ambiguous-origin coverage, separate substantive server/client codec-plan consumers including supplementary references, owned E2E line/branch coverage of 100%, final full-suite/CI runs and complete-feature/ordinary-path performance measurements. The last owned coverage measurement before these additions was 88% (4507 statements, 401 missing, 1546 branches, 242 partial); it is not a passing result. PR4111 at 65b16efe still fails combine coverage, codecov/patch and dependent test-gate; its other checks pass or intentionally skip. No S04 work has started and no merge is authorized.

Raw evidence remains in /private/tmp/dcg-s03-evidence/. The immediate next actions are publishing this fourth native-stack draft with an empty body, then closing the remaining acceptance gaps and obtaining the final cumulative Max recheck. Repository and external content remain English.

## Legacy media review and source identity checkpoint

An independent Astra Max review of cumulative `440b2f38` plus the media corrections found and verified fixes for four boundaries: shared request/response declarations returning different types at different operation/status uses; external RequestBody/Response destinations, including contentless response metadata; shared item-stream artifact dependencies; and integer YAML response keys. Capture now joins each actual media return to its exact schema use and projection. Original resolver observations identify external objects without another resolver/loader call. Only the actual stream root contributes producer dependencies to that operation's demands. A corrupted unrelated operation no longer invalidates intact types, while unscoped shared helpers retain explicit ambiguity.

Source borrowing recognizes canonical textual pointers to integer YAML keys without copying or normalizing the source. Competing string/integer keys are rejected explicitly rather than selecting a different declaration. Both Paths and Api retain existing model bytes and source objects. Item-created root fields and compact primitive arrays retain both stream projections, including boolean item schemas. Cyclic supplementary metadata fails capture and releases the failed attempt; this test supplies a real PyYAML-loaded Mapping so the oracle does not depend on the optional ryaml backend's separate cyclic-YAML rejection.

New normal-path model fixtures were generated independently from fixed main `149d933d`, with explicit filenames/settings and no output normalization. Model/type/member/source expectations are separate literal fixtures. Normal generation uses no mocks; staged-artifact mutation is confined to abnormal dependency tests.

Verification at this checkpoint:

- Full Python 3.13.2 suite: **20,216 passed, 15 skipped**, 158.60 seconds. Raw log: `/private/tmp/dcg-s03-evidence/full-media.log`.
- Focused media/source/cleanup regressions: **44 passed** on Python 3.13.2 and **44 passed** on Python 3.10.16. Logs: `legacy-media-identity.log`, `py310-media.log` in the same evidence directory.
- Independent reviewer: **45** ordinary/capture comparisons across five backends, identical bytes/warnings/original engine calls, borrowable source/member pointers, and zero retained graph. Five separate artifact-mutation comparisons verify that corrupting operation B rejects B while A remains usable. Both scopes reject ambiguous integer/string status pointers and release graphs with the exception retained. No remaining finding in this bounded review.
- Warning-strict whole-source ty 0.0.82, strict Pyright, Ruff, architecture boundaries, and store-usage guards pass.
- Additional real-generation artifact comparisons: **29 passed**. Additional abnormal producer-correspondence cases: **15 passed**. These additions follow the full-suite run and require their own final coverage run.

This is not S03 completion. The full-suite coverage report has 435 uncovered source statements, including 122 in the capture parser; overall rounded coverage is 99%, below the required owned 100% line/branch gate. The published top PR at `440b2f38` has 78 successful checks, one intentional skip, and three failures: combine coverage, dependent test-gate, and codecov/patch. PR4111 also requires its own coverage checks and review-comment resolution. Final ordinary-path and complete-product performance revalidation remains pending. Main is still `149d933d`; no stack PR has merged, and S04 has not started.

## Producer failure and nullable-alias review checkpoint

S03-3 commit `09286bc9` releases direct, caused, and contextual exception tracebacks while retaining the original latched failure object. Thirty abnormal producer-correspondence cases execute the real parser, preserve the first failure identity, and release every observed parser/ledger anchor with the exception retained. The lower branch passed 19,601 full-suite tests (15 skipped); all thirty fault cases pass on Python 3.13.2 and 3.10.16. Native `gh stack rebase --upstack` propagated the lower change without changing the top branch's existing production behavior. The top session tests now reuse the same fault inputs through the real generation driver; no normal-path replacement was introduced.

The independent Astra Max reviewer found two additional real generation defects. Recursive arrays can intentionally emit a bare list/Sequence; matching now preserves the zero-argument non-tuple form and rejects invented item arguments or empty-tuple syntax. Sixty-five permanent cases cover five backends, collapse, standard/generic containers, and rejected artifact changes. The reviewer independently confirmed forty positive and fifteen negative cases.

Nullable alias values now contribute their actual null allowance to a referencing field's None provenance. The final-field pass reuses each artifact's existing token index, corroborates producer-owned projected values and raw nullable flags, and resolves alias dependencies in linear time without recursive graph traversal or another renderer/getter call. Class-valued RootModels remain distinct. Containers do not inherit element nullability. Opaque values, unresolved cycles, and unconstrained Any aliases do not establish a nonnullable proof. Twenty permanent cases compare direct/forward/chained aliases, required references, constrained nullable unions, Any aliases, and container items across all five backends, collapse, and Annotated settings. Model bytes come from fixed main `149d933d`; provenance expectations are separate literal tables. Removed-root provenance remains conservative when the original nullable field flags are unavailable.

Verification: 1,077 affected E2E cases passed on Python 3.13.2 before the final Any refinement; 115 producer/alias/container cases passed on Python 3.10.16. The final twenty alias cases pass after adding Any coverage. The previous 844-case affected run also passed. These are subset test results, not a full-suite or coverage pass. The owned-file coverage measurement still has **262 missing statements and 254 missing branches**, out of 5,135 statements and 1,984 branches. Required coverage, final full-suite/CI, complete-product performance, and final cumulative review gates remain open. The reviewer also identified the ordinary `use_root_model_type_alias` builtin as an unsupported capture policy; its class-valued semantics require a separate regression and correction before S03 completion.

Evidence: `/private/tmp/dcg-s03-evidence/fields-complete.log`, `provenance-stack.log`, `bare-containers-all.log`, `nullable-affected.log`, `owned-alias.log`, `owned-alias-py310.log`, `nullable-alias-any.log`, and `owned-alias-coverage.json`. Main remains `149d933d`; native stack4110/PRs4108,4109,4111,4112 remain unmerged. No S04 work has started.

The bounded nullable follow-up is now verified. Producer observations retain uncertainty for Any and actual x-python-import values before module remapping; final import spelling cannot turn it into a nonnullable proof. RootModelTypeAlias uses a distinct root-alias declaration form that verifies the actual RootModel application while preserving class-valued nullability. The expanded permanent suite passes **53 cases**; the affected suite passes **1,110 cases**. The independent Astra Max reviewer reran all 53 and separately compared 12 ordinary/capture pairs (five backends plus RootModelTypeAlias, with and without Any import remapping): identical bytes and original engine/getter/resolver counts, no demand failures, zero retained graphs. No remaining finding in that bounded review. Raw logs: `root-alias-cases.log`, `owned-alias-remapped.log`; this does not close the remaining whole-stack gates.


## Inherited materialization and final-type checkpoint

S03-4 local commit `6fa48ac3` records actual no-merge inherited schema materializations, including tuples, distributed type shapes, nested reference siblings, intermediate reference chains, and x-propertyNames. The capture path observes the existing inputs and returns; it does not perform another dump, validation, merge, resolution, or cached-property evaluation. Child constraints and original parent members retain separate per-consumer origins. The nonrecursive edge insertion removes repeated descendant traversal. This correction and its parser-only regression suite are ready to propagate to S03-3 through the native stack. Published S03-3 remains `09286bc9`; published S03-4 remains `79be8135` at this checkpoint.

The independent Astra Max reviewer closed its bounded inherited-provenance findings: 65 permanent regressions passed, 35 ordinary/capture comparisons across seven fixtures and five backends preserved bytes and all profiled engine/cached-getter counts, and 15 lifetime checks retained zero parsers, schemas, or model graph objects. Independent false-branch reference-chain checks retained source ordinals across five backends. Depths 4/8/16/32 produced 20/32/56/104 unique origin edges. Literal per-consumer field tables, rather than a union across consumers, expose missing child contributions. Thirty-five additional lower-branch parser regressions use fixed-main output and the same literal field tables; the combined affected parser run passed 144 tests.

Additional accepted-session regressions cover fourteen corrupted final-type producers and thirty encoding/version/scope/backend combinations. ConstructorType now accepts only ImportedType callees, matching the finite algebra in DECISIONS-BINDING; exhaustive alias projection remains warning-strict type safe. No normal-path mock or assertion helper was added.

The full Python 3.13.2 run passed **20,532 tests with 15 skips in 161.00 seconds**, but its combined line/branch coverage was **98.88%, below 100%**, so the command failed its coverage gate. This run precedes the final cached-getter guard and thirty-five parser regressions; their affected checks passed separately. Full-source warning-strict ty 0.0.82 and strict Pyright passed after the guard. Latest published CI still fails combined coverage, dependent test-gate, and patch coverage on both PRs; the top also reports CodSpeed. There were no unresolved inline threads on PR4111 or PR4112 at the last query. No full-green claim is made.

A separate retained-memory investigation traced the previously observed residual to the existing bounded ApiModelResolver.join_path LRU cache. In a diagnostic-only comparison, clearing that cache reduced ordinary retention from 1,619,139 to 423,145 bytes and batch retention from 2,662,762 to 442,192 bytes. Production cache policy is unchanged. These measurements distinguish cached immutable strings from retained live graph objects; they do not replace final isolated timing/peak measurements. Evidence is in `/private/tmp/dcg-s03-evidence/inherited-complete.log`, `inherited-complete-coverage.json`, `retained-memory-details.json`, and `retained-cache-probe.json`.

Next: propagate the inherited correction to S03-3, finish owned E2E line/branch coverage, rerun full checks and final ordinary/full-product benchmarks, publish the native stack, address fresh CI/review feedback, and obtain a final cumulative Astra Max acceptance review. S03 is not complete; S04 has not started.

## Continued independent review and counterexamples

The native stack was successfully rebased and submitted with S03-3 at `93ed08fd` and S03-4 at `31c5149a`; stack 4110 and all four PRs remain unmerged. Lower-branch tests use its parser-owned field origins without depending on the later field freezer. The independent Astra Max reviewer rechecked cumulative S03 plus the exercised S01/S02 boundaries. Its 43 integration tests and 40 warmed generation comparisons preserved model bytes, original engine/getter counts, and zero retained parser/schema/ledger graphs.

The follow-up changes address independently reproduced conditional namespace overwrites, dynamic export aliases, TypedDict extra-items import overrides and transitive demand dependencies, constrained array/object union root origins and full-property helper precedence, nested mapping-value root provenance, and response encoding-header directions. Partial branch observations are classified from the actual active producer identities; they do not change original schema origins or suppress unrelated ambiguous uses. Existing builtin generation is called once. Final artifact inspection rejects dynamic execution without executing generated modules, and excludes uncalled function bodies. Independent literal tables and fixed-main byte fixtures cover the new cases, with abnormal producer corruption limited to failure tests.

Current checks: 471 final-artifact/constrained-union/encoding/model cases passed, and 301 producer-correspondence and module-scope cases passed before the last three executable-statement additions. Whole-source warning-strict ty and strict Pyright pass. The latest full Python 3.13 run had 20,735 passes, 15 skips, 32 failures and 48 setup errors; all 80 unsuccessful cases required local HTTP/HTTPS sockets blocked by the execution sandbox. Repeating their five containing modules with local socket access passed 6,184 cases with three skips. This is affected-run evidence, not a new all-green full-run claim. The full run still had 221 uncovered owned source statements and 223 missing owned branches before the final follow-up tests. Coverage remains an open gate.

Evidence: `/private/tmp/dcg-s03-evidence/review-full.log`, `review-full-coverage.json`, `review-network.log`, `review-extended.log`, `review-guards.log`, and `baseline-constrained-unions.log`. Latest published CI on both draft PRs still fails combined/patch coverage and the dependent test gate; the top PR also reports CodSpeed. The current changes require a further independent recheck, complete owned coverage, final coherent full checks and isolated performance measurements. S03 is not complete; no S04 work has started.

## Assigned-call and failure-boundary follow-up

Artifact inspection now rejects unsupported calls inside module assignments and control expressions, including parenthesized and indexed callees. It permits the finite imported builtin annotation factories and ordinary model rebuild statements without executing generated code. The independent Astra Max reviewer closed this finding with 45 artifact probes across five backends and 30 normal comparisons: unchanged bytes and original engine calls, clean normal demands, one factory read, and zero retained parser/graph nodes. All previously reported correctness findings are addressed within the reviewed scope; the reviewer explicitly did not approve the remaining coverage, CI, or performance gates.

The affected suite passed 1,617 tests with the assigned-call correction and 18 backend failure cases. Six additional source-correspondence faults now run through both the real parser and shared generation session: 78 cases passed, with exact failure messages, no accepted failed batch, preserved first-failure identity, and released graphs. Fault injection is limited to abnormal producer/state corruption; normal generation and the existing assertion helper are unchanged. Evidence: `/private/tmp/dcg-s03-evidence/owned-followup.log`, `owned-followup-coverage.json`, and `origin-failures.log` (the latter records the initial fixture-scope failure; the corrected 78-case rerun passed).

Native stack 4110 remains open. This checkpoint does not satisfy the owned 100% line/branch coverage gate, final coherent full-suite/CI gate, or final isolated performance gate. S04 has not started.

## Final-record and default-policy verification

The next affected run passed **1,776 tests in 25.18 seconds**. It covers corrupted module ownership, unsupported model/template policies, lost extra-items observations, foreign functional inheritance, unresolved emitted references, mixed field-attempt identities, malformed retained type expressions, and legacy observation loss. Normal paths use actual generation; only failure cases alter completed observations. The owned source gate is still open: **118 missing statements and 150 missing branches** remain in the coherent `owned-additions-coverage.json` measurement.

Unknown adopted model parameters/configuration now produce a symbol-scoped `BND_CUSTOM_BINDING_REQUIRED` while retaining their opaque facts. Independent Astra Max review passed ten five-backend fault probes and eleven ordinary configured/alias-generator comparisons, with unrelated models still usable, equal generated bytes and original engine calls, and zero retained graphs. Its native-runtime facts regression selection passed eleven tests. No actionable finding remained in that bounded change.

Ten normal default-policy cases independently distinguish equal-valued explicit overrides, schema default:null, nullable declarations, optional fallbacks, and force-optional configuration across all five backends. Their literal provenance tables are separate from fixed-main generated bytes. Ten forward-discriminator/module-split cases preserve original engine calls and bytes. Fixed main `149d933d` already omits cross-module enum imports in the enum-member variant for four backends; the new binding correctly rejects those unresolved emitted annotations instead of inventing imports or changing ordinary output. Literal-discriminator and TypedDict variants remain usable.

Evidence: `record-failures.log`, `declaration-demands.log`, `final-type-guard.log`, `baseline-default-policies.log`, `default-policies.log`, `baseline-split-discriminators.log`, `split-discriminators.log`, and `owned-additions.log` in `/private/tmp/dcg-s03-evidence`. A fresh PR check found the pre-commit bot's formatting-only top commit `4b9c2d07` and one CodeQL unused-type-import comment on PR4111; both need incorporation before the next native submission. Final full-suite, lower-PR coverage, CI and isolated performance remain incomplete. S04 has not started.


## Import producer identity and repeated-attempt review

The current S03-4 work follows published `368d52a1`; published S03-3 remains `571c4faa`. Main was fetched again and remains `149d933d927ca12f2e0e45fc18f8a4cf772046d7`. Native stack 4110 and PRs 4108, 4109, 4111, and 4112 are open and unmerged. No S04 work has started.

Independent Astra Max review exposed three import-provenance counterexamples: operation-only SerializeAsAny must retain its producer import when no model imports that wrapper; a same-spelled external import is not evidence that the policy wrapper was emitted; and failed enclosing field projection must not hide an actual adopted leaf import. A fourth case combines dotted shared modules, tree reuse, and recursive exports: ordinary rendering replaces ModuleContext wrappers, so wrapper identity cannot own the retained import evidence. Parser-global x-python-import entries also require their own pre-remap evidence.

The capture-only boundary now observes actual completed model.imports return tuples at the existing classmethod collector, snapshots parser-global import storage before ordinary remapping, and corroborates original identities against their owning final import collections. It calls each ordinary boundary once and adds no getter evaluation, import rendering, model mutation, or ordinary-path observer. The context value contains scalar model identities and immutable import tuples; finally resets the context and clears its mutable contents, including copies retained by copy_context. Recording failures are latched before repair suppression; ordinary engine exceptions preserve existing handling. Import evidence follows the actual Imports collection, which survives ordinary export-path wrapper replacement. This is the concrete producer-identity refinement required by B01/B06/B11; it does not relax accepted-artifact corroboration.

The independent reviewer passed 72 committed-shape regression cases and 29 separately authored normal comparisons across five backends, including a partial-field helper without any parser-global UUID import. Ordinary bytes and observed engine call counts matched; retained parser and graph counts were zero. It independently checked classmethod forwarding, copied-context cleanup, observer failure identity and traceback cleanup, rejected recording failures, and ordinary failed-repair fallback to attempt 1. No confirmed correctness finding remains in this bounded follow-up; coverage, CI and performance are explicitly not approved by that result.

A coherent full Python 3.13 run passed **21,228 tests with 15 skips in 185.23 seconds**, but the command failed the 100% coverage gate at **99.40%** (348 missing statements and 233 missing branches overall; 96 missing owned statements and 135 missing owned branches). Whole-source warning-strict ty and strict Pyright passed after correcting the imported classmethod annotations. A subsequent targeted run passed **139 tests in 9.17 seconds**, adding the independent five-backend partial-helper case, ordinary import-collector failure controls, and raw backend facts. Those later tests are not included in the coherent full-run count. Normal paths use real generation, the existing assert_output helper, external input/oracle files, and fixed-main output fixtures. Fault injection is confined to abnormal state or exceptions.

Current published PR4112 has 78 successful checks, one intentional skip, and three failed checks: combine coverage, dependent test-gate, and codecov/patch. Neither PR4111 nor PR4112 has an unresolved review thread at this inspection. The lower CodeQL comment was addressed and resolved with the concrete type-narrowing correction. An isolated 17-case, 30-pair A/A performance control has completed: every 99% paired interval includes zero and no sign test is significant at alpha 0.01. The final 60-pair A/B and full-product timing/peak measurements remain in progress.

Evidence: import-fix-full.log, import-fix-full-coverage.json, import-complete.log, baseline-global-imports.log, baseline-imported-helper.log, baseline-split-collapse.log, common-parser.log, and import-fix-aa.json under /private/tmp/dcg-s03-evidence. Next: finish lower-PR independent coverage and remaining owned branches, complete isolated performance measurements, publish the native stack, and close fresh CI/review gates before declaring S03 complete.


## Stack completion checkpoint

Main remains `149d933d927ca12f2e0e45fc18f8a4cf772046d7`. Native stack 4110 keeps the four PRs in order: #4108 `generation-platform-binding-uses`, #4109 `generation-platform-binding-replacements`, #4111 `generation-platform-binding-fields` and #4112 `generation-platform-binding-freeze`. Nothing has been merged and S04 has not started.

Layer changes since the previous checkpoint:

- #4108 and #4109 keep their behavior; explanatory line comments moved into docstrings or were removed.
- #4111 removes guards that the capture invariants make unreachable; an unexpected state now reaches the existing latched `BindingCaptureError` instead of a silent fallback. It fixes three capture-only failures found by comparing ordinary and captured generation: boolean `true` branches in concatenated combined lists, inline `allOf` unions whose producer frame was not the direct loader, and dangling `$ref` loader keys. Unknown alias nullability now yields an `opaque` annotation origin instead of `none`, and the unused ellipsis tuple form is gone. An independent review found that one removed matcher branch was reachable: msgspec emits `Union[Annotated[Union[...], Meta(...)], UnsetType]` for described optional unions, so the branch that matches a field-level union enclosing the complete projected union is restored and pinned by `emitted-meta.json`; a mixed `allOf: [$ref, {oneOf: ...}]` case is pinned as well.
- #4112 is one squashed commit for the previously published S03-4 history plus the import-producer follow-up, then focused commits. Plain `anyOf`/`oneOf` schema helpers are no longer ambiguous because origins reached only through combined-materialization edges are secondary candidates. `SerializeAsAny` reference policies follow the new neutral `DataType.SUPPORTS_SERIALIZE_AS_ANY` capability, so non-pydantic backends bind the plain reference they emit. Recursive roots that ordinary generation collapses away report `BND_SYMBOL_NOT_EMITTED` instead of failing the attempt. Legacy request bodies and responses follow `$ref` only for occurrences the engine actually resolved by reference: operation-level `get_ref_model` calls are attributed to the request body or, in engine order, to referenced responses, and both schema origins and wire declarations use that record, so a `$ref` with inline siblings that the engine read inline keeps its inline declaration. Legacy scopes ignore non-mapping path entries such as global `paths.parameters`. Every artifact definition keeps its source statement, and unreachable guards were removed.

Verification:

- Owned modules of all four layers have 100% line and branch coverage from end-to-end tests. New inputs and oracles are external files; fault injection stays limited to abnormal states. Every new generated-code oracle equals fixed-main ordinary output byte for byte.
- #4112 full Python 3.13 suite: 21,357 passed and 15 skipped. The changed-test set passes 8,013 tests on Python 3.13 (8,012 on 3.10 and 3.14 before the final review fixes). Ruff, formatting, warning-strict ty, strict Pyright and the architecture/store guards pass.
- Ordinary versus capture sweep over every repository OpenAPI fixture: 3,060 parser runs across five option sets have no output or latched-capture difference (361 fail identically on both paths). 3,672 complete-session runs across six option sets differ only where `session-cyclic-metadata.yaml` intentionally rejects recursive source metadata. The contract-edge test snapshots each use's declaration, schema site, media, state and bound type.
- Ordinary-path A/B against fixed main (17 cases, 60 balanced pairs, seed 4098): no wall or CPU 99% interval lies above zero. The complete capture product (generation, freeze, artifact validation and close) compared with the previously published #4112 head plus its import follow-up (30 balanced pairs): the two contract cases differ by 0.00 and +0.14 ms, and the large OpenAPI case by +4.3 ms of 1,077 ms. Every 99% interval includes zero.

Conservative behavior kept on purpose: a module whose ordinary output spells a dynamic builtin name, for example the enum default `Mode.eval`, is reported as `BND_TYPE_EXPRESSION_UNSUPPORTED` rather than corroborated. Two ordinary-generator issues were observed outside S03 and left unchanged: a discriminator field can be dropped with `use_enum_values_in_discriminator` and `module_split_mode="single"`, and one module-split output imports `Literal` without using it.
