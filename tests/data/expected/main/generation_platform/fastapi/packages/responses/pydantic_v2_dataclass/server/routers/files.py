# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the files operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Final

from fastapi import APIRouter
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.responses import respond
from ..services import FilesService


def _add_download(router: APIRouter, wiring: Wiring) -> None:
    download_handler = wiring.handlers['download']

    def download() -> Response:
        return respond(download_handler(), contract.Download.RESPONSES)

    router.add_api_route(
        '/download',
        download,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='download',
        tags=['files'],
        response_description='The file.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1download/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1download/get'),
    )


LITERAL_ROUTES: Final = ((contract.Download.OPERATION, _add_download),)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    files: FilesService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the files operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'files': files},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
