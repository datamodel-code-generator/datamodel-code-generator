# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the results operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Final

import models
from fastapi import APIRouter
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.responses import dispatch, respond
from ..services import ResultsService


def _add_create(router: APIRouter, wiring: Wiring) -> None:
    create_handler = wiring.handlers['create']

    def create() -> Response:
        return respond(create_handler(), contract.Create.RESPONSES)

    router.add_api_route(
        '/created',
        create,
        methods=['POST'],
        status_code=201,
        response_model=None,
        response_class=Response,
        operation_id='create',
        tags=['results'],
        response_description='Created.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1created/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1created/post'),
    )


def _add_only_default(router: APIRouter, wiring: Wiring) -> None:
    only_default_handler = wiring.handlers['only_default']

    def only_default() -> Response:
        return respond(only_default_handler(), contract.OnlyDefault.RESPONSES)

    router.add_api_route(
        '/defaults',
        only_default,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='onlyDefault',
        tags=['results'],
        response_description='Anything.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1defaults/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1defaults/get'),
    )


def _add_only_ranges(router: APIRouter, wiring: Wiring) -> None:
    only_ranges_handler = wiring.handlers['only_ranges']

    def only_ranges() -> Response:
        return respond(only_ranges_handler(), contract.OnlyRanges.RESPONSES)

    router.add_api_route(
        '/ranges',
        only_ranges,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='onlyRanges',
        tags=['results'],
        response_description='Success.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1ranges/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1ranges/get'),
    )


def _add_only_errors(router: APIRouter, wiring: Wiring) -> None:
    only_errors_handler = wiring.handlers['only_errors']

    def only_errors() -> object:
        return dispatch(only_errors_handler(), contract.OnlyErrors.RESPONSES)

    router.add_api_route(
        '/errors',
        only_errors,
        methods=['GET'],
        status_code=404,
        response_model=models.Problem,
        response_model_by_alias=True,
        response_model_exclude_unset=True,
        operation_id='onlyErrors',
        tags=['results'],
        response_description='Missing.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1errors/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1errors/get'),
    )


def _add_get_optional(router: APIRouter, wiring: Wiring) -> None:
    get_optional_handler = wiring.handlers['get_optional']

    def get_optional() -> Response:
        return respond(get_optional_handler(), contract.GetOptional.RESPONSES)

    router.add_api_route(
        '/optional',
        get_optional,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='getOptional',
        tags=['results'],
        response_description='A loose thing.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1optional/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1optional/get'),
    )


def _add_get_secret(router: APIRouter, wiring: Wiring) -> None:
    get_secret_handler = wiring.handlers['get_secret']

    def get_secret() -> Response:
        return respond(get_secret_handler(), contract.GetSecret.RESPONSES)

    router.add_api_route(
        '/secret',
        get_secret,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='getSecret',
        tags=['results'],
        response_description='A thing with a write-only field.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1secret/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1secret/get'),
    )


def _add_head_secret(router: APIRouter, wiring: Wiring) -> None:
    head_secret_handler = wiring.handlers['head_secret']

    def head_secret() -> Response:
        return respond(head_secret_handler(), contract.HeadSecret.RESPONSES)

    router.add_api_route(
        '/secret',
        head_secret,
        methods=['HEAD'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='headSecret',
        tags=['results'],
        response_description='Exists.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1secret/head',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1secret/head'),
    )


LITERAL_ROUTES: Final = (
    (contract.Create.OPERATION, _add_create),
    (contract.OnlyDefault.OPERATION, _add_only_default),
    (contract.OnlyRanges.OPERATION, _add_only_ranges),
    (contract.OnlyErrors.OPERATION, _add_only_errors),
    (contract.GetOptional.OPERATION, _add_get_optional),
    (contract.GetSecret.OPERATION, _add_get_secret),
    (contract.HeadSecret.OPERATION, _add_head_secret),
)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    results: ResultsService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the results operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'results': results},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
