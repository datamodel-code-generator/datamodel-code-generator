# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the untagged operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Annotated, Final

import models
from fastapi import APIRouter, Body, Depends
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.responses import respond
from ..services import UntaggedService


def _add_post_discriminated(router: APIRouter, wiring: Wiring) -> None:
    post_discriminated_handler = wiring.handlers['post_discriminated']

    def post_discriminated(
        *,
        body: Annotated[models.Discriminated, Depends(contract.PostDiscriminated.BODY)],
    ) -> Response:
        return respond(
            post_discriminated_handler(body=body),
            contract.PostDiscriminated.RESPONSES,
        )

    router.add_api_route(
        '/discriminated',
        post_discriminated,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postDiscriminated',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1discriminated/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1discriminated/post'),
    )


def _add_post_ranges(router: APIRouter, wiring: Wiring) -> None:
    post_ranges_handler = wiring.handlers['post_ranges']

    def post_ranges(
        *,
        body: Annotated[models.Ranges, Body(media_type='application/json')],
    ) -> Response:
        return respond(post_ranges_handler(body=body), contract.PostRanges.RESPONSES)

    router.add_api_route(
        '/ranges',
        post_ranges,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postRanges',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1ranges/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1ranges/post'),
    )


def _add_post_wide(router: APIRouter, wiring: Wiring) -> None:
    post_wide_handler = wiring.handlers['post_wide']

    def post_wide(
        *,
        body: Annotated[models.Wide, Depends(contract.PostWide.BODY)],
    ) -> Response:
        return respond(post_wide_handler(body=body), contract.PostWide.RESPONSES)

    router.add_api_route(
        '/wide',
        post_wide,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postWide',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1wide/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1wide/post'),
    )


def _add_post_described(router: APIRouter, wiring: Wiring) -> None:
    post_described_handler = wiring.handlers['post_described']

    def post_described(
        *,
        body: Annotated[models.Described, Depends(contract.PostDescribed.BODY)],
    ) -> Response:
        return respond(
            post_described_handler(body=body),
            contract.PostDescribed.RESPONSES,
        )

    router.add_api_route(
        '/described',
        post_described,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postDescribed',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1described/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1described/post'),
    )


def _add_post_patterned(router: APIRouter, wiring: Wiring) -> None:
    post_patterned_handler = wiring.handlers['post_patterned']

    def post_patterned(
        *,
        body: Annotated[models.Patterned, Depends(contract.PostPatterned.BODY)],
    ) -> Response:
        return respond(
            post_patterned_handler(body=body),
            contract.PostPatterned.RESPONSES,
        )

    router.add_api_route(
        '/patterned',
        post_patterned,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postPatterned',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1patterned/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1patterned/post'),
    )


def _add_post_bounded(router: APIRouter, wiring: Wiring) -> None:
    post_bounded_handler = wiring.handlers['post_bounded']

    def post_bounded(
        *,
        body: Annotated[models.Bounded, Depends(contract.PostBounded.BODY)],
    ) -> Response:
        return respond(post_bounded_handler(body=body), contract.PostBounded.RESPONSES)

    router.add_api_route(
        '/bounded',
        post_bounded,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postBounded',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1bounded/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1bounded/post'),
    )


def _add_post_painted(router: APIRouter, wiring: Wiring) -> None:
    post_painted_handler = wiring.handlers['post_painted']

    def post_painted(
        *,
        body: Annotated[models.Painted, Body(media_type='application/json')],
    ) -> Response:
        return respond(post_painted_handler(body=body), contract.PostPainted.RESPONSES)

    router.add_api_route(
        '/painted',
        post_painted,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postPainted',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1painted/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1painted/post'),
    )


def _add_post_holder(router: APIRouter, wiring: Wiring) -> None:
    post_holder_handler = wiring.handlers['post_holder']

    def post_holder(
        *,
        body: Annotated[models.Holder, Body(media_type='application/json')],
    ) -> Response:
        return respond(post_holder_handler(body=body), contract.PostHolder.RESPONSES)

    router.add_api_route(
        '/holder',
        post_holder,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postHolder',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1holder/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1holder/post'),
    )


def _add_post_wrapper(router: APIRouter, wiring: Wiring) -> None:
    post_wrapper_handler = wiring.handlers['post_wrapper']

    def post_wrapper(
        *,
        body: Annotated[models.Wrapper, Depends(contract.PostWrapper.BODY)],
    ) -> Response:
        return respond(post_wrapper_handler(body=body), contract.PostWrapper.RESPONSES)

    router.add_api_route(
        '/wrapper',
        post_wrapper,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postWrapper',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1wrapper/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1wrapper/post'),
    )


def _add_post_extended(router: APIRouter, wiring: Wiring) -> None:
    post_extended_handler = wiring.handlers['post_extended']

    def post_extended(
        *,
        body: Annotated[models.Extended, Depends(contract.PostExtended.BODY)],
    ) -> Response:
        return respond(
            post_extended_handler(body=body),
            contract.PostExtended.RESPONSES,
        )

    router.add_api_route(
        '/extended',
        post_extended,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postExtended',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1extended/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1extended/post'),
    )


def _add_post_defaulted(router: APIRouter, wiring: Wiring) -> None:
    post_defaulted_handler = wiring.handlers['post_defaulted']

    def post_defaulted(
        *,
        body: Annotated[models.Defaulted, Depends(contract.PostDefaulted.BODY)],
    ) -> Response:
        return respond(
            post_defaulted_handler(body=body),
            contract.PostDefaulted.RESPONSES,
        )

    router.add_api_route(
        '/defaulted',
        post_defaulted,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postDefaulted',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1defaulted/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1defaulted/post'),
    )


def _add_post_listed(router: APIRouter, wiring: Wiring) -> None:
    post_listed_handler = wiring.handlers['post_listed']

    def post_listed(
        *,
        body: Annotated[models.Listed, Depends(contract.PostListed.BODY)],
    ) -> Response:
        return respond(post_listed_handler(body=body), contract.PostListed.RESPONSES)

    router.add_api_route(
        '/listed',
        post_listed,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postListed',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1listed/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1listed/post'),
    )


def _add_post_mapped(router: APIRouter, wiring: Wiring) -> None:
    post_mapped_handler = wiring.handlers['post_mapped']

    def post_mapped(
        *,
        body: Annotated[models.Mapped, Depends(contract.PostMapped.BODY)],
    ) -> Response:
        return respond(post_mapped_handler(body=body), contract.PostMapped.RESPONSES)

    router.add_api_route(
        '/mapped',
        post_mapped,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postMapped',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1mapped/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1mapped/post'),
    )


def _add_post_opaque(router: APIRouter, wiring: Wiring) -> None:
    post_opaque_handler = wiring.handlers['post_opaque']

    def post_opaque(
        *,
        body: Annotated[models.Opaque, Depends(contract.PostOpaque.BODY)],
    ) -> Response:
        return respond(post_opaque_handler(body=body), contract.PostOpaque.RESPONSES)

    router.add_api_route(
        '/opaque',
        post_opaque,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postOpaque',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1opaque/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1opaque/post'),
    )


def _add_post_anything(router: APIRouter, wiring: Wiring) -> None:
    post_anything_handler = wiring.handlers['post_anything']

    def post_anything(
        *,
        body: Annotated[models.Anything, Depends(contract.PostAnything.BODY)],
    ) -> Response:
        return respond(
            post_anything_handler(body=body),
            contract.PostAnything.RESPONSES,
        )

    router.add_api_route(
        '/anything',
        post_anything,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postAnything',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1anything/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1anything/post'),
    )


def _add_post_constant(router: APIRouter, wiring: Wiring) -> None:
    post_constant_handler = wiring.handlers['post_constant']

    def post_constant(
        *,
        body: Annotated[models.Constant, Body(media_type='application/json')],
    ) -> Response:
        return respond(
            post_constant_handler(body=body),
            contract.PostConstant.RESPONSES,
        )

    router.add_api_route(
        '/constant',
        post_constant,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postConstant',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1constant/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1constant/post'),
    )


def _add_post_stamped(router: APIRouter, wiring: Wiring) -> None:
    post_stamped_handler = wiring.handlers['post_stamped']

    def post_stamped(
        *,
        body: Annotated[models.Stamped, Depends(contract.PostStamped.BODY)],
    ) -> Response:
        return respond(post_stamped_handler(body=body), contract.PostStamped.RESPONSES)

    router.add_api_route(
        '/stamped',
        post_stamped,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postStamped',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1stamped/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1stamped/post'),
    )


def _add_post_closed(router: APIRouter, wiring: Wiring) -> None:
    post_closed_handler = wiring.handlers['post_closed']

    def post_closed(
        *,
        body: Annotated[models.Closed, Body(media_type='application/json')],
    ) -> Response:
        return respond(post_closed_handler(body=body), contract.PostClosed.RESPONSES)

    router.add_api_route(
        '/closed',
        post_closed,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postClosed',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1closed/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1closed/post'),
    )


def _add_post_twice(router: APIRouter, wiring: Wiring) -> None:
    post_twice_handler = wiring.handlers['post_twice']

    def post_twice(
        *,
        body: Annotated[models.Twice, Depends(contract.PostTwice.BODY)],
    ) -> Response:
        return respond(post_twice_handler(body=body), contract.PostTwice.RESPONSES)

    router.add_api_route(
        '/twice',
        post_twice,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postTwice',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1twice/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1twice/post'),
    )


def _add_post_node(router: APIRouter, wiring: Wiring) -> None:
    post_node_handler = wiring.handlers['post_node']

    def post_node(
        *,
        body: Annotated[models.Node, Body(media_type='application/json')],
    ) -> Response:
        return respond(post_node_handler(body=body), contract.PostNode.RESPONSES)

    router.add_api_route(
        '/node',
        post_node,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postNode',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1node/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1node/post'),
    )


def _add_post_extra(router: APIRouter, wiring: Wiring) -> None:
    post_extra_handler = wiring.handlers['post_extra']

    def post_extra(
        *,
        body: Annotated[models.Extra, Depends(contract.PostExtra.BODY)],
    ) -> Response:
        return respond(post_extra_handler(body=body), contract.PostExtra.RESPONSES)

    router.add_api_route(
        '/extra',
        post_extra,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postExtra',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1extra/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1extra/post'),
    )


def _add_post_nested(router: APIRouter, wiring: Wiring) -> None:
    post_nested_handler = wiring.handlers['post_nested']

    def post_nested(
        *,
        body: Annotated[models.Nested, Depends(contract.PostNested.BODY)],
    ) -> Response:
        return respond(post_nested_handler(body=body), contract.PostNested.RESPONSES)

    router.add_api_route(
        '/nested',
        post_nested,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postNested',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1nested/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1nested/post'),
    )


def _add_post_box(router: APIRouter, wiring: Wiring) -> None:
    post_box_handler = wiring.handlers['post_box']

    def post_box(
        *,
        body: Annotated[models.Box, Depends(contract.PostBox.BODY)],
    ) -> Response:
        return respond(post_box_handler(body=body), contract.PostBox.RESPONSES)

    router.add_api_route(
        '/box',
        post_box,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postBox',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1box/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1box/post'),
    )


LITERAL_ROUTES: Final = (
    (contract.PostDiscriminated.OPERATION, _add_post_discriminated),
    (contract.PostRanges.OPERATION, _add_post_ranges),
    (contract.PostWide.OPERATION, _add_post_wide),
    (contract.PostDescribed.OPERATION, _add_post_described),
    (contract.PostPatterned.OPERATION, _add_post_patterned),
    (contract.PostBounded.OPERATION, _add_post_bounded),
    (contract.PostPainted.OPERATION, _add_post_painted),
    (contract.PostHolder.OPERATION, _add_post_holder),
    (contract.PostWrapper.OPERATION, _add_post_wrapper),
    (contract.PostExtended.OPERATION, _add_post_extended),
    (contract.PostDefaulted.OPERATION, _add_post_defaulted),
    (contract.PostListed.OPERATION, _add_post_listed),
    (contract.PostMapped.OPERATION, _add_post_mapped),
    (contract.PostOpaque.OPERATION, _add_post_opaque),
    (contract.PostAnything.OPERATION, _add_post_anything),
    (contract.PostConstant.OPERATION, _add_post_constant),
    (contract.PostStamped.OPERATION, _add_post_stamped),
    (contract.PostClosed.OPERATION, _add_post_closed),
    (contract.PostTwice.OPERATION, _add_post_twice),
    (contract.PostNode.OPERATION, _add_post_node),
    (contract.PostExtra.OPERATION, _add_post_extra),
    (contract.PostNested.OPERATION, _add_post_nested),
    (contract.PostBox.OPERATION, _add_post_box),
)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    untagged: UntaggedService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the untagged operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'untagged': untagged},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
