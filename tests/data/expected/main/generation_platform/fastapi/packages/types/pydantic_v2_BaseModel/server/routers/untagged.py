# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the untagged operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Annotated, Final, Literal

from fastapi import APIRouter, Depends, Query
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.requests import absent, present
from .._runtime.server.responses import respond
from ..services import UntaggedService


def _add_get_values(router: APIRouter, wiring: Wiring) -> None:
    get_values_handler = wiring.handlers['get_values']

    def get_values(
        *,
        code: Annotated[str, Query(alias='code', max_length=3, default_factory=absent)],
        tags: Annotated[list[str], Query(alias='tags', default_factory=absent)],
        kind: Annotated[Literal['a', 'b'], Query(alias='kind', default_factory=absent)],
        parameters: Annotated[contract.GetValues.Parameters, Depends(contract.GetValues.PARAMETERS)],
    ) -> Response:
        return respond(
            get_values_handler(
                code=present(code),
                tags=present(tags),
                kind=present(kind),
                at=parameters.at,
                stamp=parameters.stamp,
                count=parameters.count,
                host=parameters.host,
                label=parameters.label,
                point=parameters.point,
                raw=parameters.raw,
                blob=parameters.blob,
            ),
            contract.GetValues.RESPONSES,
        )

    router.add_api_route(
        '/values',
        get_values,
        methods=['GET'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='getValues',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1values/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1values/get'),
    )


LITERAL_ROUTES: Final = ((contract.GetValues.OPERATION, _add_get_values),)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    untagged: UntaggedService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the untagged operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'untagged': untagged},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
