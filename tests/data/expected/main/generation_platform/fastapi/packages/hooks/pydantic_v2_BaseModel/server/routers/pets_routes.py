# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the pets_routes operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Annotated, Final

import models
from fastapi import APIRouter, Body, Depends, Header, Path, Query, Request
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.requests import absent, present
from .._runtime.server.responses import dispatch, respond
from .._runtime.server.security import PrincipalT, SecretT
from ..auth_types import AsyncAuthorizer, Authorizer, CredentialExtractors
from ..services import PetsRoutesService


def _add_handle_add_pet(router: APIRouter, wiring: Wiring) -> None:
    handle_add_pet_handler = wiring.async_handlers['handle_add_pet']
    handle_add_pet_principal = wiring.authenticators['/paths/~1pets/post']

    async def handle_add_pet(
        *,
        request: Request,
        principal: Annotated[object, Depends(handle_add_pet_principal)],
        body: Annotated[models.Pet, Body(media_type='application/json')],
    ) -> object:
        return dispatch(
            await handle_add_pet_handler(
                request=request,
                principal=principal,
                body=body,
            ),
            contract.HandleAddPet.RESPONSES,
        )

    router.add_api_route(
        '/pets',
        handle_add_pet,
        methods=['POST'],
        status_code=201,
        response_model=models.Pet,
        response_model_by_alias=True,
        response_model_exclude_unset=True,
        operation_id='addPet',
        tags=['pets'],
        deprecated=True,
        response_description='Added.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1pets/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1pets/post'),
    )


def _add_handle_get_pet(router: APIRouter, wiring: Wiring) -> None:
    handle_get_pet_handler = wiring.async_handlers['handle_get_pet']
    handle_get_pet_principal = wiring.authenticators['/paths/~1pets~1{petId}/get']

    async def handle_get_pet(
        *,
        request: Request,
        principal: Annotated[object, Depends(handle_get_pet_principal)],
        pet_id: Annotated[int, Path(alias='petId', ge=1)],
        limit: Annotated[int, Query(alias='limit')] = 10,
        x_trace: Annotated[str, Header(
            alias='X-Trace',
            convert_underscores=False,
            default_factory=absent,
        )],
        parameters: Annotated[contract.HandleGetPet.Parameters, Depends(contract.HandleGetPet.PARAMETERS)],
    ) -> Response:
        return respond(
            await handle_get_pet_handler(
                request=request,
                principal=principal,
                pet_id=pet_id,
                limit=limit,
                x_trace=present(x_trace),
                after=parameters.after,
                session=parameters.session,
            ),
            contract.HandleGetPet.RESPONSES,
        )

    router.add_api_route(
        '/pets/{petId}',
        handle_get_pet,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='getPet',
        tags=['pets'],
        summary='Read one pet.',
        description='Reads a pet by identifier.',
        response_description='The pet.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1pets~1{petId}/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1pets~1{petId}/get'),
    )


LITERAL_ROUTES: Final = ((contract.HandleAddPet.OPERATION, _add_handle_add_pet),)
TEMPLATED_ROUTES: Final = ((contract.HandleGetPet.OPERATION, _add_handle_get_pet),)


def build_router(
    *,
    pets_routes: PetsRoutesService[PrincipalT],
    authorizer: Authorizer[SecretT, PrincipalT] | AsyncAuthorizer[SecretT, PrincipalT],
    credential_extractors: CredentialExtractors[SecretT] | None = None,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the pets_routes operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'pets_routes': pets_routes},
        authorizer=authorizer,
        credential_extractors=credential_extractors,
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
