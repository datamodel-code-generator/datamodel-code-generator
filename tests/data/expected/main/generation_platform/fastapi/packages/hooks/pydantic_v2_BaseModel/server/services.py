# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Service interfaces of this package's router groups; regenerate them instead of editing."""

from __future__ import annotations

from abc import abstractmethod
from typing import Protocol

import models
from fastapi import Request, UploadFile
from fastapi.responses import Response
from typing_extensions import TypeVar

from .model_codecs import ModelValue, Unset
from .responses import (
    HandleAddPetResponsePayload,
    HandleGetPetResponsePayload,
    HandlePostUploadsResponsePayload,
    HandlePutNotesResponsePayload,
    HTTPResult,
)

PrincipalT_contra = TypeVar("PrincipalT_contra", contravariant=True)


class UntaggedRoutesService(Protocol[PrincipalT_contra]):
    """Implement the untagged_routes operations: subclass this Protocol, or give an object its methods."""

    @abstractmethod
    async def handle_put__notes(
        self,
        *,
        request: Request,
        principal: PrincipalT_contra | None,
        body: models.FieldNotesPutRequest | models.FieldNotesPutRequest1 | Unset,
        media_type: str | None,
    ) -> None | HTTPResult[HandlePutNotesResponsePayload] | Response: ...


class FilesRoutesService(Protocol):
    """Implement the files_routes operations: subclass this Protocol, or give an object its methods."""

    @abstractmethod
    async def handle_post__uploads(
        self,
        *,
        request: Request,
        file: UploadFile,
        note: str | Unset,
    ) -> None | HTTPResult[HandlePostUploadsResponsePayload] | Response: ...


class PetsRoutesService(Protocol[PrincipalT_contra]):
    """Implement the pets_routes operations: subclass this Protocol, or give an object its methods."""

    @abstractmethod
    async def handle_add_pet(
        self,
        *,
        request: Request,
        principal: PrincipalT_contra,
        body: models.Pet,
    ) -> (
        models.Pet
        | ModelValue[models.Pet]
        | HTTPResult[HandleAddPetResponsePayload]
        | Response
    ): ...

    @abstractmethod
    async def handle_get_pet(
        self,
        *,
        request: Request,
        principal: PrincipalT_contra,
        pet_id: int,
        limit: int,
        x_trace: str | Unset,
        after: models.FieldPetsPetIdGetQueryAfterParameter | Unset,
        session: models.FieldPetsPetIdGetCookieSessionParameter | Unset,
    ) -> (
        models.Pet
        | ModelValue[models.Pet]
        | HTTPResult[HandleGetPetResponsePayload]
        | Response
    ): ...
