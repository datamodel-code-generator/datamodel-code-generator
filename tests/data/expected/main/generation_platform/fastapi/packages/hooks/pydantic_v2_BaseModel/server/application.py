# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Build the router and the application of every selected operation from the services that implement them."""

from collections.abc import Sequence
from typing import Final

from fastapi import APIRouter, FastAPI

from ._generated import contract, openapi
from ._generated.contract import OperationDependencies, OperationKey, SchemeKey
from ._runtime.server.application import (
    Dependency,
    FastAPIOptions,
    application_options,
    build,
)
from ._runtime.server.openapi import install
from ._runtime.server.security import PrincipalT, SecretT
from .auth_types import (
    AsyncAuthorizer,
    AsyncCredentialExtractor,
    Authorizer,
    CredentialExtractor,
    CredentialExtractors,
)
from .routers import files_routes, pets_routes, untagged_routes
from .services import FilesRoutesService, PetsRoutesService, UntaggedRoutesService

ROUTES: Final = (
    *untagged_routes.LITERAL_ROUTES,
    *files_routes.LITERAL_ROUTES,
    *pets_routes.LITERAL_ROUTES,
    *untagged_routes.TEMPLATED_ROUTES,
    *files_routes.TEMPLATED_ROUTES,
    *pets_routes.TEMPLATED_ROUTES,
)
INFO: Final[FastAPIOptions] = {'title': 'Hooked pets', 'version': '1.0'}


def build_router(
    *,
    untagged_routes: UntaggedRoutesService[PrincipalT],
    files_routes: FilesRoutesService,
    pets_routes: PetsRoutesService[PrincipalT],
    authorizer: Authorizer[SecretT, PrincipalT] | AsyncAuthorizer[SecretT, PrincipalT],
    credential_extractors: CredentialExtractors[SecretT] | None = None,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the services and settings, then register every operation, literal paths first."""
    return build(
        ROUTES,
        contract.SCHEMES,
        services={
            'untagged_routes': untagged_routes,
            'files_routes': files_routes,
            'pets_routes': pets_routes,
        },
        authorizer=authorizer,
        credential_extractors=credential_extractors,
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )


def install_openapi(
    app: FastAPI,
    *,
    operation_keys: tuple[OperationKey, ...] | None = None,
) -> None:
    """Add this package's OpenAPI fragments to an application that includes its routes.

    operation_keys names the operations it includes, all of them by default. The
    application composes its document when it first serves or checks it.
    """
    install(app, openapi.PLAN, operation_keys)


def create_app(
    *,
    untagged_routes: UntaggedRoutesService[PrincipalT],
    files_routes: FilesRoutesService,
    pets_routes: PetsRoutesService[PrincipalT],
    authorizer: Authorizer[SecretT, PrincipalT] | AsyncAuthorizer[SecretT, PrincipalT],
    credential_extractors: CredentialExtractors[SecretT] | None = None,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
    fastapi_options: FastAPIOptions | None = None,
) -> FastAPI:
    """Create the application with every operation, then check its OpenAPI document.

    The check keeps no document, so the one the application serves also covers routes added afterwards.
    """
    app = FastAPI(**application_options(fastapi_options, INFO))
    app.include_router(
        build_router(
            untagged_routes=untagged_routes,
            files_routes=files_routes,
            pets_routes=pets_routes,
            authorizer=authorizer,
            credential_extractors=credential_extractors,
            dependencies=dependencies,
            operation_dependencies=operation_dependencies,
            prefix=prefix,
        )
    )
    install_openapi(app)
    app.openapi()
    app.openapi_schema = None
    return app


__all__ = [
    'AsyncAuthorizer',
    'AsyncCredentialExtractor',
    'Authorizer',
    'CredentialExtractor',
    'CredentialExtractors',
    'Dependency',
    'FastAPIOptions',
    'OperationDependencies',
    'OperationKey',
    'SchemeKey',
    'build_router',
    'create_app',
    'install_openapi',
]
