# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the users operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Annotated, Final

from fastapi import APIRouter, Depends, Form, Header, Path, Query
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.requests import absent, present
from .._runtime.server.responses import respond
from ..services import UsersService


def _add_get__users_me(router: APIRouter, wiring: Wiring) -> None:
    get__users_me_handler = wiring.handlers['get__users_me']

    def get__users_me() -> Response:
        return respond(get__users_me_handler(), contract.GetUsersMe.RESPONSES)

    router.add_api_route(
        '/users/me',
        get__users_me,
        methods=['GET'],
        status_code=204,
        response_model=None,
        response_class=Response,
        tags=['Users', 'admin'],
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1users~1me/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1users~1me/get'),
    )


def _add_get_user_by_id(router: APIRouter, wiring: Wiring) -> None:
    get_user_by_id_handler_1 = wiring.handlers['get_user_by_id']

    def get_user_by_id(
        *,
        path_id: Annotated[int, Path(alias='id')],
        query_id: Annotated[int, Query(alias='id', default_factory=absent)],
        query_body: Annotated[str, Query(alias='body', default_factory=absent)],
        header_media_type: Annotated[str, Header(
            alias='media_type',
            convert_underscores=False,
            default_factory=absent,
        )],
        dcg_p0: Annotated[str, Query(alias='dcg_p0', default_factory=absent)],
        parameters: Annotated[str, Query(alias='parameters', default_factory=absent)],
        get_user_by_id_handler: Annotated[str, Query(
            alias='get_user_by_id_handler',
            default_factory=absent,
        )],
        parameters_1: Annotated[contract.GetUserById.Parameters, Depends(contract.GetUserById.PARAMETERS)],
    ) -> Response:
        return respond(
            get_user_by_id_handler_1(
                path_id=path_id,
                query_id=present(query_id),
                query_body=present(query_body),
                header_media_type=present(header_media_type),
                dcg_p0=present(dcg_p0),
                parameters=present(parameters),
                session=parameters_1.session,
                get_user_by_id_handler=present(get_user_by_id_handler),
            ),
            contract.GetUserById.RESPONSES,
        )

    router.add_api_route(
        '/users/{id}',
        get_user_by_id,
        methods=['GET'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='GetUserByID',
        tags=['Users'],
        deprecated=True,
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1users~1{id}/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1users~1{id}/get'),
    )


def _add_class_(router: APIRouter, wiring: Wiring) -> None:
    class__handler = wiring.handlers['class_']

    def class_(
        *,
        path_id: Annotated[int, Path(alias='id')],
        query_principal: Annotated[str, Query(
            alias='principal',
            default_factory=absent,
        )],
        form_id: Annotated[str, Form(alias='id', default_factory=absent)],
    ) -> Response:
        return respond(
            class__handler(
                path_id=path_id,
                query_principal=present(query_principal),
                form_id=present(form_id),
            ),
            contract.Class.RESPONSES,
        )

    router.add_api_route(
        '/users/{id}',
        class_,
        methods=['PUT'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='class',
        tags=['Users'],
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1users~1{id}/put',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1users~1{id}/put'),
    )


LITERAL_ROUTES: Final = ((contract.GetUsersMe.OPERATION, _add_get__users_me),)
TEMPLATED_ROUTES: Final = (
    (contract.GetUserById.OPERATION, _add_get_user_by_id),
    (contract.Class.OPERATION, _add_class_),
)


def build_router(
    *,
    users: UsersService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the users operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'users': users},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
