# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the router_2024_files operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Annotated, Final

from fastapi import APIRouter, Path, Query
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.requests import absent, present
from .._runtime.server.responses import respond
from ..services import Router2024FilesService


def _add_get_file(router: APIRouter, wiring: Wiring) -> None:
    get_file_handler = wiring.handlers['get_file']

    def get_file(
        *,
        file_name: Annotated[str, Path(alias='dcg_p1')],
        dcg_p0: Annotated[str, Path(alias='dcg_p0')],
        page_size: Annotated[int, Query(alias='limit', default_factory=absent)],
    ) -> Response:
        return respond(
            get_file_handler(
                file_name=file_name,
                dcg_p0=dcg_p0,
                page_size=present(page_size),
            ),
            contract.GetFile.RESPONSES,
        )

    router.add_api_route(
        '/files/{dcg_p1}/{dcg_p0}',
        get_file,
        methods=['GET'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='getFile',
        tags=['2024 files'],
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1files~1{file.name}~1{dcg_p0}/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1files~1{file.name}~1{dcg_p0}/get'),
    )


LITERAL_ROUTES: Final = ()
TEMPLATED_ROUTES: Final = ((contract.GetFile.OPERATION, _add_get_file),)


def build_router(
    *,
    router_2024_files: Router2024FilesService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the router_2024_files operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'router_2024_files': router_2024_files},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
