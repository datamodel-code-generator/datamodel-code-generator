# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the misc operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Final

from fastapi import APIRouter
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.responses import respond
from ..services import MiscService


def _add_health_check(router: APIRouter, wiring: Wiring) -> None:
    health_check_handler = wiring.handlers['health_check']

    def health_check() -> Response:
        return respond(health_check_handler(), contract.HealthCheck.RESPONSES)

    router.add_api_route(
        '/health',
        health_check,
        methods=['GET'],
        status_code=204,
        response_model=None,
        response_class=Response,
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1health/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1health/get'),
    )


LITERAL_ROUTES: Final = ((contract.HealthCheck.OPERATION, _add_health_check),)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    misc: MiscService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the misc operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'misc': misc},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
