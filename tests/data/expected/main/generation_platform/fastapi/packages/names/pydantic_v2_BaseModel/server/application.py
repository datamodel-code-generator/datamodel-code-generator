# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Build the router and the application of every selected operation from the services that implement them."""

from collections.abc import Sequence
from typing import Final

from fastapi import APIRouter, FastAPI

from ._generated import contract, openapi
from ._generated.contract import OperationDependencies, OperationKey, SchemeKey
from ._runtime.server.application import (
    Dependency,
    FastAPIOptions,
    application_options,
    build,
)
from ._runtime.server.openapi import install
from .auth_types import (
    AsyncAuthorizer,
    AsyncCredentialExtractor,
    Authorizer,
    CredentialExtractor,
    CredentialExtractors,
)
from .routers import misc, router_2024_files, users
from .services import MiscService, Router2024FilesService, UsersService

ROUTES: Final = (
    *users.LITERAL_ROUTES,
    *router_2024_files.LITERAL_ROUTES,
    *misc.LITERAL_ROUTES,
    *users.TEMPLATED_ROUTES,
    *router_2024_files.TEMPLATED_ROUTES,
    *misc.TEMPLATED_ROUTES,
)
INFO: Final[FastAPIOptions] = {'title': 'Names', 'version': '1.0'}


def build_router(
    *,
    users: UsersService,
    router_2024_files: Router2024FilesService,
    misc: MiscService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the services and settings, then register every operation, literal paths first."""
    return build(
        ROUTES,
        contract.SCHEMES,
        services={'users': users, 'router_2024_files': router_2024_files, 'misc': misc},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )


def install_openapi(
    app: FastAPI,
    *,
    operation_keys: tuple[OperationKey, ...] | None = None,
) -> None:
    """Add this package's OpenAPI fragments to an application that includes its routes.

    operation_keys names the operations it includes, all of them by default. The
    application composes its document when it first serves or checks it.
    """
    install(app, openapi.PLAN, operation_keys)


def create_app(
    *,
    users: UsersService,
    router_2024_files: Router2024FilesService,
    misc: MiscService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
    fastapi_options: FastAPIOptions | None = None,
) -> FastAPI:
    """Create the application with every operation, then check its OpenAPI document."""
    app = FastAPI(**application_options(fastapi_options, INFO))
    app.include_router(
        build_router(
            users=users,
            router_2024_files=router_2024_files,
            misc=misc,
            dependencies=dependencies,
            operation_dependencies=operation_dependencies,
            prefix=prefix,
        )
    )
    install_openapi(app)
    app.openapi()
    return app


__all__ = [
    'AsyncAuthorizer',
    'AsyncCredentialExtractor',
    'Authorizer',
    'CredentialExtractor',
    'CredentialExtractors',
    'Dependency',
    'FastAPIOptions',
    'OperationDependencies',
    'OperationKey',
    'SchemeKey',
    'build_router',
    'create_app',
    'install_openapi',
]
