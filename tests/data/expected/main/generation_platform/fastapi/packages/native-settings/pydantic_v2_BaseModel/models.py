# generated by datamodel-codegen:
#   filename:  native-settings.yaml

from __future__ import annotations

from decimal import Decimal
from typing import Any

from pydantic import AliasChoices, BaseModel, ConfigDict, Field, RootModel
from pydantic.experimental.missing_sentinel import MISSING


class Named(BaseModel):
    model_config = ConfigDict(
        validate_by_name=True,
    )
    my_name: str = Field(..., alias='my-name')


class Titled(BaseModel):
    model_config = ConfigDict(
        validate_by_name=True,
    )
    title: str = Field(..., validation_alias=AliasChoices('title', 'heading'))


class Strict(BaseModel):
    model_config = ConfigDict(
        validate_by_name=True,
    )
    count: int = Field(..., strict=True)


class Amount(RootModel[Decimal]):
    root: Decimal = Field(..., json_schema_extra={'x-python-type': 'decimal.Decimal'})


class Loose(BaseModel):
    model_config = ConfigDict(
        validate_by_name=True,
    )
    name: str | MISSING = MISSING


class Holder(BaseModel):
    model_config = ConfigDict(
        validate_by_name=True,
    )
    amount: Amount
    extra: dict[str, Any]
