# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the untagged operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Annotated, Final

import models
from fastapi import APIRouter, Depends
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.responses import dispatch, respond
from ..services import UntaggedService


def _add_post_named(router: APIRouter, wiring: Wiring) -> None:
    post_named_handler = wiring.handlers['post_named']

    def post_named(
        *,
        body: Annotated[models.Named, Depends(contract.PostNamed.BODY)],
    ) -> Response:
        return respond(post_named_handler(body=body), contract.PostNamed.RESPONSES)

    router.add_api_route(
        '/named',
        post_named,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postNamed',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1named/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1named/post'),
    )


def _add_post_titled(router: APIRouter, wiring: Wiring) -> None:
    post_titled_handler = wiring.handlers['post_titled']

    def post_titled(
        *,
        body: Annotated[models.Titled, Depends(contract.PostTitled.BODY)],
    ) -> Response:
        return respond(post_titled_handler(body=body), contract.PostTitled.RESPONSES)

    router.add_api_route(
        '/titled',
        post_titled,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postTitled',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1titled/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1titled/post'),
    )


def _add_post_strict(router: APIRouter, wiring: Wiring) -> None:
    post_strict_handler = wiring.handlers['post_strict']

    def post_strict(
        *,
        body: Annotated[models.Strict, Depends(contract.PostStrict.BODY)],
    ) -> Response:
        return respond(post_strict_handler(body=body), contract.PostStrict.RESPONSES)

    router.add_api_route(
        '/strict',
        post_strict,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postStrict',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1strict/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1strict/post'),
    )


def _add_post_wrapped(router: APIRouter, wiring: Wiring) -> None:
    post_wrapped_handler = wiring.handlers['post_wrapped']

    def post_wrapped(
        *,
        body: Annotated[models.Holder, Depends(contract.PostWrapped.BODY)],
    ) -> Response:
        return respond(post_wrapped_handler(body=body), contract.PostWrapped.RESPONSES)

    router.add_api_route(
        '/wrapped',
        post_wrapped,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postWrapped',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1wrapped/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1wrapped/post'),
    )


def _add_get_loose(router: APIRouter, wiring: Wiring) -> None:
    get_loose_handler = wiring.handlers['get_loose']

    def get_loose() -> object:
        return dispatch(get_loose_handler(), contract.GetLoose.RESPONSES)

    router.add_api_route(
        '/loose',
        get_loose,
        methods=['GET'],
        status_code=200,
        response_model=models.Loose,
        response_model_by_alias=True,
        response_model_exclude_unset=True,
        operation_id='getLoose',
        response_description='A loose value.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1loose/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1loose/get'),
    )


LITERAL_ROUTES: Final = (
    (contract.PostNamed.OPERATION, _add_post_named),
    (contract.PostTitled.OPERATION, _add_post_titled),
    (contract.PostStrict.OPERATION, _add_post_strict),
    (contract.PostWrapped.OPERATION, _add_post_wrapped),
    (contract.GetLoose.OPERATION, _add_get_loose),
)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    untagged: UntaggedService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the untagged operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'untagged': untagged},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
