# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Service interfaces of this package's router groups; regenerate them instead of editing."""

from __future__ import annotations

from abc import abstractmethod
from typing import Protocol

from fastapi.responses import Response

from .responses import HTTPResult, PingResponsePayload


class Service(Protocol):
    """Implement every operation: subclass this Protocol, or give an object its methods."""

    @abstractmethod
    def ping(self) -> None | HTTPResult[PingResponsePayload] | Response: ...
