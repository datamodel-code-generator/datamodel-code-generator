# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Service interfaces of this package's router groups; regenerate them instead of editing."""

from __future__ import annotations

from abc import abstractmethod
from typing import Protocol

import models
from fastapi import Request, UploadFile
from fastapi.responses import Response
from typing_extensions import TypeVar

from .model_codecs import ModelValue, Unset
from .responses import (
    FilesPostResponsePayload,
    HandlePostRawResponsePayload,
    HandlePutNotesResponsePayload,
    HTTPResult,
    PetsGetResponsePayload,
    PetsPostResponsePayload,
)

PrincipalT_contra = TypeVar("PrincipalT_contra", contravariant=True)


class PetsService(Protocol[PrincipalT_contra]):
    """Implement the pets operations: subclass this Protocol, or give an object its methods."""

    @abstractmethod
    def pets_get(
        self,
        *,
        request: Request,
        principal: PrincipalT_contra,
        pet_id: int,
        limit: int,
        x_trace: str | Unset,
        after: models.FieldPetsPetIdGetQueryAfterParameter | Unset,
        session: models.FieldPetsPetIdGetCookieSessionParameter | Unset,
    ) -> (
        models.Pet
        | ModelValue[models.Pet]
        | HTTPResult[PetsGetResponsePayload]
        | Response
    ): ...

    @abstractmethod
    def pets_post(
        self,
        *,
        request: Request,
        principal: PrincipalT_contra,
        body: models.Pet,
    ) -> (
        models.Pet
        | ModelValue[models.Pet]
        | HTTPResult[PetsPostResponsePayload]
        | Response
    ): ...


class FilesService(Protocol):
    """Implement the files operations: subclass this Protocol, or give an object its methods."""

    @abstractmethod
    def files_post(
        self,
        *,
        request: Request,
        file: UploadFile,
        note: str | Unset,
    ) -> None | HTTPResult[FilesPostResponsePayload] | Response: ...


class UntaggedService(Protocol[PrincipalT_contra]):
    """Implement the untagged operations: subclass this Protocol, or give an object its methods."""

    @abstractmethod
    def handle_put__notes(
        self,
        *,
        request: Request,
        principal: PrincipalT_contra | None,
        body: models.FieldNotesPutRequest | models.FieldNotesPutRequest1 | Unset,
        media_type: str | None,
    ) -> None | HTTPResult[HandlePutNotesResponsePayload] | Response: ...

    @abstractmethod
    def handle_post__raw(
        self,
        *,
        request: Request,
    ) -> None | HTTPResult[HandlePostRawResponsePayload] | Response: ...
