# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the untagged operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Annotated, Final

import models
from fastapi import APIRouter, Depends, Request
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.model_codecs.unset import Unset
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.responses import respond
from .._runtime.server.security import PrincipalT, SecretT
from ..auth_types import AsyncAuthorizer, Authorizer, CredentialExtractors
from ..services import UntaggedService


def _add_handle_put__notes(router: APIRouter, wiring: Wiring) -> None:
    handle_put__notes_handler = wiring.handlers['handle_put__notes']
    handle_put__notes_principal = wiring.authenticators['/paths/~1notes/put']

    def handle_put__notes(
        *,
        request: Request,
        principal: Annotated[object, Depends(handle_put__notes_principal)],
        body: Annotated[tuple[str | None, models.FieldNotesPutRequest | models.FieldNotesPutRequest1 | Unset], Depends(contract.HandlePutNotes.BODY.receive)],
    ) -> Response:
        return respond(
            handle_put__notes_handler(
                request=request,
                principal=principal,
                body=body[1],
                media_type=body[0],
            ),
            contract.HandlePutNotes.RESPONSES,
        )

    router.add_api_route(
        '/notes',
        handle_put__notes,
        methods=['PUT'],
        status_code=204,
        response_model=None,
        response_class=Response,
        response_description='Saved.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1notes/put',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1notes/put'),
    )


def _add_handle_post__raw(router: APIRouter, wiring: Wiring) -> None:
    handle_post__raw_handler = wiring.handlers['handle_post__raw']

    def handle_post__raw(*, request: Request) -> Response:
        return respond(
            handle_post__raw_handler(request=request),
            contract.HandlePostRaw.RESPONSES,
        )

    router.add_api_route(
        '/raw',
        handle_post__raw,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        response_description='Accepted.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1raw/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1raw/post'),
    )


LITERAL_ROUTES: Final = (
    (contract.HandlePutNotes.OPERATION, _add_handle_put__notes),
    (contract.HandlePostRaw.OPERATION, _add_handle_post__raw),
)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    untagged: UntaggedService[PrincipalT],
    authorizer: Authorizer[SecretT, PrincipalT] | AsyncAuthorizer[SecretT, PrincipalT],
    credential_extractors: CredentialExtractors[SecretT] | None = None,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the untagged operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'untagged': untagged},
        authorizer=authorizer,
        credential_extractors=credential_extractors,
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
