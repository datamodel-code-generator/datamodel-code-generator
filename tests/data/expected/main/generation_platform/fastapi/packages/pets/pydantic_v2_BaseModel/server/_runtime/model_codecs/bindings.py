# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Static per-use model bindings, planned at generation time and resolved to native types at startup."""

from __future__ import annotations

from dataclasses import dataclass
from typing import Literal, TypeAlias

from .context import Direction  # noqa: TC001 - Public annotations support get_type_hints().

BackendId: TypeAlias = Literal[
    "pydantic_v2.BaseModel", "pydantic_v2.dataclass", "dataclasses.dataclass", "typing.TypedDict", "msgspec.Struct"
]
NativeKind: TypeAlias = Literal[
    "model", "dataclass", "typed_dict", "struct", "scalar", "enum", "literal", "union", "array", "map", "root", "alias"
]
ConverterStrategy: TypeAlias = Literal[
    "pydantic_type_adapter",
    "dataclass_structural",
    "typeddict_structural",
    "msgspec_convert",
    "msgspec_structural",
    "registered_adapter",
]
ProjectionMode: TypeAlias = Literal["native", "envelope"]
ExtraPolicy: TypeAlias = Literal["ignore", "allow", "forbid"]
Representation: TypeAlias = Literal["value", "decimal_string"]


@dataclass(frozen=True, slots=True)
class ModelNode:
    """Refer to a generated model binding by its symbol key, so recursive graphs stay finite."""

    symbol: str


@dataclass(frozen=True, slots=True)
class ArrayNode:
    """A homogeneous wire array projected into one native sequence or set kind."""

    item: TypeNode
    container: Literal["list", "tuple", "set", "frozenset"] = "list"


@dataclass(frozen=True, slots=True)
class TupleNode:
    """A fixed-length wire array with one type per position."""

    items: tuple[TypeNode, ...]


@dataclass(frozen=True, slots=True)
class MapNode:
    """A wire object with arbitrary member names and one value type."""

    value: TypeNode


@dataclass(frozen=True, slots=True)
class UnionNode:
    """The final emitted non-null union members, in native order, and whether null is a member."""

    members: tuple[TypeNode, ...]
    nullable: bool = False


@dataclass(frozen=True, slots=True)
class LeafNode:
    """A value the codec never walks into, with the wire representation of ambiguous native scalars."""

    representation: Representation = "value"


TypeNode: TypeAlias = ModelNode | ArrayNode | TupleNode | MapNode | UnionNode | LeafNode


@dataclass(frozen=True, slots=True, kw_only=True)
class FieldBinding:
    """Join one wire property to the native attribute, the input keys it reads, and the key the codec uses."""

    field_id: str
    native_name: str
    wire_name: str
    validation_key: str
    validation_keys: tuple[str, ...]
    required: bool
    read_only: bool
    write_only: bool
    omit_none: bool
    type: TypeNode


@dataclass(frozen=True, slots=True, kw_only=True)
class ModelBinding:
    """Describe one generated model: its fields, unknown-property policy, and root value."""

    symbol: str
    native_kind: Literal["model", "dataclass", "root"]
    schema_id: str | None
    fields: tuple[FieldBinding, ...] = ()
    root: TypeNode | None = None
    extra: ExtraPolicy = "ignore"
    open: bool = True


@dataclass(frozen=True, slots=True, kw_only=True)
class UseBinding:
    """Bind one directional type use to its schema view, native type graph, and conversion plan."""

    binding_id: str
    direction: Direction
    schema_id: str
    operation_id: str | None
    media_type: str | None
    backend: BackendId
    native_kind: NativeKind
    native_export: str | None
    projection_mode: ProjectionMode
    converter_strategy: ConverterStrategy
    type: TypeNode
    models: tuple[ModelBinding, ...]
