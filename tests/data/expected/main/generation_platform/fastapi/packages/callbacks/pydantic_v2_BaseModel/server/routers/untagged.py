# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the untagged operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Annotated, Final

import models
from fastapi import APIRouter, Depends
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.responses import respond
from .._runtime.server.security import PrincipalT, SecretT
from ..auth_types import AsyncAuthorizer, Authorizer, CredentialExtractors
from ..services import UntaggedService


def _add_subscribe(router: APIRouter, wiring: Wiring) -> None:
    subscribe_handler = wiring.handlers['subscribe']

    def subscribe(
        *,
        body: Annotated[models.Subscription, Depends(contract.Subscribe.BODY)],
    ) -> Response:
        return respond(subscribe_handler(body=body), contract.Subscribe.RESPONSES)

    router.add_api_route(
        '/subscriptions',
        subscribe,
        methods=['POST'],
        status_code=201,
        response_model=None,
        response_class=Response,
        operation_id='subscribe',
        response_description='Subscribed.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1subscriptions/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1subscriptions/post'),
    )


def _add_list_events(router: APIRouter, wiring: Wiring) -> None:
    list_events_handler = wiring.handlers['list_events']

    def list_events() -> Response:
        return respond(list_events_handler(), contract.ListEvents.RESPONSES)

    router.add_api_route(
        '/events',
        list_events,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='listEvents',
        response_description='The events.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1events/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1events/get'),
    )


def _add_get_secure(router: APIRouter, wiring: Wiring) -> None:
    get_secure_handler = wiring.handlers['get_secure']
    get_secure_principal = wiring.authenticators['/paths/~1secure/get']

    def get_secure(
        *,
        principal: Annotated[object, Depends(get_secure_principal)],
        parameters: Annotated[contract.GetSecure.Parameters, Depends(contract.GetSecure.PARAMETERS)],
    ) -> Response:
        return respond(
            get_secure_handler(principal=principal, token=parameters.token),
            contract.GetSecure.RESPONSES,
        )

    router.add_api_route(
        '/secure',
        get_secure,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='getSecure',
        response_description='The animal.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1secure/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1secure/get'),
    )


def _add_ping(router: APIRouter, wiring: Wiring) -> None:
    ping_handler = wiring.handlers['ping']

    def ping() -> Response:
        return respond(ping_handler(), contract.Ping.RESPONSES)

    router.add_api_route(
        '/ping',
        ping,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='ping',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1ping/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1ping/get'),
    )


def _add_echo(router: APIRouter, wiring: Wiring) -> None:
    echo_handler = wiring.handlers['echo']

    def echo() -> Response:
        return respond(echo_handler(), contract.Echo.RESPONSES)

    router.add_api_route(
        '/echo',
        echo,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='echo',
        response_description='The echo.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'server',
                'operation': '/paths/~1echo/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1echo/get'),
    )


LITERAL_ROUTES: Final = (
    (contract.Subscribe.OPERATION, _add_subscribe),
    (contract.ListEvents.OPERATION, _add_list_events),
    (contract.GetSecure.OPERATION, _add_get_secure),
    (contract.Ping.OPERATION, _add_ping),
    (contract.Echo.OPERATION, _add_echo),
)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    untagged: UntaggedService[PrincipalT],
    authorizer: Authorizer[SecretT, PrincipalT] | AsyncAuthorizer[SecretT, PrincipalT],
    credential_extractors: CredentialExtractors[SecretT] | None = None,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the untagged operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'untagged': untagged},
        authorizer=authorizer,
        credential_extractors=credential_extractors,
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
