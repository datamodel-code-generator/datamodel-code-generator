# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the untagged operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Annotated, Final

import results_basemodel_models
from fastapi import APIRouter, Query
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.responses import dispatch, respond
from ..services import UntaggedService


def _add_get_result(router: APIRouter, wiring: Wiring) -> None:
    get_result_handler = wiring.handlers['get_result']

    def get_result(*, case: Annotated[str, Query(alias='case')]) -> object:
        return dispatch(get_result_handler(case=case), contract.GetResult.RESPONSES)

    router.add_api_route(
        '/results',
        get_result,
        methods=['GET'],
        status_code=200,
        response_model=results_basemodel_models.Thing,
        response_model_by_alias=True,
        response_model_exclude_unset=True,
        operation_id='getResult',
        response_description='A thing.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'results_basemodel',
                'operation': '/paths/~1results/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1results/get'),
    )


def _add_head_result(router: APIRouter, wiring: Wiring) -> None:
    head_result_handler = wiring.handlers['head_result']

    def head_result(*, case: Annotated[str, Query(alias='case')]) -> Response:
        return respond(head_result_handler(case=case), contract.HeadResult.RESPONSES)

    router.add_api_route(
        '/results',
        head_result,
        methods=['HEAD'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='headResult',
        response_description='Exists.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'results_basemodel',
                'operation': '/paths/~1results/head',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1results/head'),
    )


def _add_get_plain(router: APIRouter, wiring: Wiring) -> None:
    get_plain_handler = wiring.handlers['get_plain']

    def get_plain(*, case: Annotated[str, Query(alias='case')]) -> Response:
        return respond(get_plain_handler(case=case), contract.GetPlain.RESPONSES)

    router.add_api_route(
        '/plain',
        get_plain,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='getPlain',
        response_description='A short text.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'results_basemodel',
                'operation': '/paths/~1plain/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1plain/get'),
    )


def _add_get_latin(router: APIRouter, wiring: Wiring) -> None:
    get_latin_handler = wiring.handlers['get_latin']

    def get_latin(*, case: Annotated[str, Query(alias='case')]) -> Response:
        return respond(get_latin_handler(case=case), contract.GetLatin.RESPONSES)

    router.add_api_route(
        '/latin',
        get_latin,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='getLatin',
        response_description='A Latin-1 text.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'results_basemodel',
                'operation': '/paths/~1latin/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1latin/get'),
    )


def _add_get_nothing(router: APIRouter, wiring: Wiring) -> None:
    get_nothing_handler = wiring.handlers['get_nothing']

    def get_nothing(*, case: Annotated[str, Query(alias='case')]) -> Response:
        return respond(get_nothing_handler(case=case), contract.GetNothing.RESPONSES)

    router.add_api_route(
        '/nothing',
        get_nothing,
        methods=['GET'],
        status_code=200,
        response_model=None,
        response_class=Response,
        operation_id='getNothing',
        response_description='Anything.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'results_basemodel',
                'operation': '/paths/~1nothing/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1nothing/get'),
    )


def _add_post_empty(router: APIRouter, wiring: Wiring) -> None:
    post_empty_handler = wiring.handlers['post_empty']

    def post_empty(*, case: Annotated[str, Query(alias='case')]) -> Response:
        return respond(post_empty_handler(case=case), contract.PostEmpty.RESPONSES)

    router.add_api_route(
        '/empty',
        post_empty,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postEmpty',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'results_basemodel',
                'operation': '/paths/~1empty/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1empty/post'),
    )


LITERAL_ROUTES: Final = (
    (contract.GetResult.OPERATION, _add_get_result),
    (contract.HeadResult.OPERATION, _add_head_result),
    (contract.GetPlain.OPERATION, _add_get_plain),
    (contract.GetLatin.OPERATION, _add_get_latin),
    (contract.GetNothing.OPERATION, _add_get_nothing),
    (contract.PostEmpty.OPERATION, _add_post_empty),
)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    untagged: UntaggedService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the untagged operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'untagged': untagged},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
