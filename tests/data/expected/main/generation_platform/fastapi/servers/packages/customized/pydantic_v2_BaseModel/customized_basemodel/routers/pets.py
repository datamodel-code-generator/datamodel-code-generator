# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the pets operations; regenerate them instead of editing.

Router tag:pets of Services, tagged pets, with 3 operations.
"""

from collections.abc import Sequence
from typing import Annotated, Final

import customized_basemodel_models
from fastapi import APIRouter, Path
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.responses import dispatch, respond
from ..services import PetsService


def _add_handle_list_pets(router: APIRouter, wiring: Wiring) -> None:
    handle_list_pets_handler = wiring.async_handlers['handle_list_pets']

    async def handle_list_pets() -> object:
        return dispatch(
            await handle_list_pets_handler(),
            contract.HandleListPets.RESPONSES,
        )

    router.add_api_route(
        '/pets',
        handle_list_pets,
        methods=['GET'],
        status_code=200,
        response_model=customized_basemodel_models.FieldPetsGetResponse,
        response_model_by_alias=True,
        response_model_exclude_unset=True,
        operation_id='listPets',
        tags=['pets'],
        response_description='Names.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'customized_basemodel',
                'operation': '/paths/~1pets/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1pets/get'),
    )


def _add_handle_delete_pet(router: APIRouter, wiring: Wiring) -> None:
    handle_delete_pet_handler = wiring.async_handlers['handle_delete_pet']

    async def handle_delete_pet(
        *,
        pet_id: Annotated[int, Path(alias='petId')],
    ) -> Response:
        return respond(
            await handle_delete_pet_handler(pet_id=pet_id),
            contract.HandleDeletePet.RESPONSES,
        )

    router.add_api_route(
        '/pets/{petId}',
        handle_delete_pet,
        methods=['DELETE'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='deletePet',
        tags=['pets'],
        response_description='Deleted.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'customized_basemodel',
                'operation': '/paths/~1pets~1{petId}/delete',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1pets~1{petId}/delete'),
    )


def _add_handle_move_pet(router: APIRouter, wiring: Wiring) -> None:
    handle_move_pet_handler = wiring.async_handlers['handle_move_pet']

    async def handle_move_pet(
        *,
        pet_id: Annotated[int, Path(alias='petId')],
    ) -> Response:
        return respond(
            await handle_move_pet_handler(pet_id=pet_id),
            contract.HandleMovePet.RESPONSES,
        )

    router.add_api_route(
        '/pets/{petId}/moves',
        handle_move_pet,
        methods=['POST'],
        status_code=201,
        response_model=None,
        response_class=Response,
        operation_id='movePet',
        tags=['pets'],
        response_description='Moved.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'customized_basemodel',
                'operation': '/paths/~1pets~1{petId}~1moves/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1pets~1{petId}~1moves/post'),
    )


LITERAL_ROUTES: Final = ((contract.HandleListPets.OPERATION, _add_handle_list_pets),)
TEMPLATED_ROUTES: Final = (
    (contract.HandleDeletePet.OPERATION, _add_handle_delete_pet),
    (contract.HandleMovePet.OPERATION, _add_handle_move_pet),
)


def build_router(
    *,
    pets: PetsService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the pets operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'pets': pets},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
