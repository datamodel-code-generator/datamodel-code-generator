# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the untagged operations; regenerate them instead of editing."""

from collections.abc import Sequence
from typing import Annotated, Final, Literal

import responses_basemodel_models
from fastapi import APIRouter, Path, Query

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.requests import absent, present
from .._runtime.server.responses import dispatch
from ..services import UntaggedService


def _add_get_greeting(router: APIRouter, wiring: Wiring) -> None:
    get_greeting_handler = wiring.handlers['get_greeting']

    def get_greeting(
        *,
        mode: Annotated[Literal['bare', 'result', 'value'], Query(
            alias='mode',
            default_factory=absent,
        )],
    ) -> object:
        return dispatch(
            get_greeting_handler(mode=present(mode)),
            contract.GetGreeting.RESPONSES,
        )

    router.add_api_route(
        '/greetings',
        get_greeting,
        methods=['GET'],
        status_code=200,
        response_model=responses_basemodel_models.FieldGreetingsGetResponse,
        response_model_by_alias=True,
        response_model_exclude_unset=True,
        operation_id='getGreeting',
        response_description='The greeting.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'responses_basemodel',
                'operation': '/paths/~1greetings/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1greetings/get'),
    )


def _add_get_document(router: APIRouter, wiring: Wiring) -> None:
    get_document_handler = wiring.handlers['get_document']

    def get_document(*, id: Annotated[int, Path(alias='id')]) -> object:
        return dispatch(get_document_handler(id=id), contract.GetDocument.RESPONSES)

    router.add_api_route(
        '/documents/{id}',
        get_document,
        methods=['GET'],
        status_code=200,
        response_model=responses_basemodel_models.Pet,
        response_model_by_alias=True,
        response_model_exclude_unset=True,
        operation_id='getDocument',
        response_description='The document, as a pet or as raw bytes.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'responses_basemodel',
                'operation': '/paths/~1documents~1{id}/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1documents~1{id}/get'),
    )


def _add_get_pet(router: APIRouter, wiring: Wiring) -> None:
    get_pet_handler = wiring.handlers['get_pet']

    def get_pet(*, id: Annotated[int, Path(alias='id')]) -> object:
        return dispatch(get_pet_handler(id=id), contract.GetPet.RESPONSES)

    router.add_api_route(
        '/pets/{id}',
        get_pet,
        methods=['GET'],
        status_code=200,
        response_model=responses_basemodel_models.Pet,
        response_model_by_alias=True,
        response_model_exclude_unset=True,
        operation_id='getPet',
        response_description='The pet.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'responses_basemodel',
                'operation': '/paths/~1pets~1{id}/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1pets~1{id}/get'),
    )


LITERAL_ROUTES: Final = ((contract.GetGreeting.OPERATION, _add_get_greeting),)
TEMPLATED_ROUTES: Final = (
    (contract.GetDocument.OPERATION, _add_get_document),
    (contract.GetPet.OPERATION, _add_get_pet),
)


def build_router(
    *,
    untagged: UntaggedService,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Check the service and settings, then register the untagged operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        services={'untagged': untagged},
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
    )
