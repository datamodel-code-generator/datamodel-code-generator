# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the untagged operations; regenerate them instead of editing."""

from collections.abc import Callable, Mapping, Sequence
from typing import Final

import unscaffolded_basemodel_models
from fastapi import APIRouter
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.responses import dispatch, respond
from .._runtime.server.security import SecretT
from ..auth_types import AsyncAuthorizer, Authorizer, CredentialExtractors


def _add_list_pets(router: APIRouter, wiring: Wiring) -> None:
    list_pets_handler = wiring.handlers['list_pets']

    def list_pets() -> object:
        return dispatch(list_pets_handler(), contract.ListPets.RESPONSES)

    router.add_api_route(
        '/pets',
        list_pets,
        methods=['GET'],
        status_code=200,
        response_model=unscaffolded_basemodel_models.FieldPetsGetResponse,
        response_model_by_alias=True,
        response_model_exclude_unset=True,
        operation_id='listPets',
        response_description='Names.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'unscaffolded_basemodel',
                'operation': '/paths/~1pets/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1pets/get'),
    )


def _add_get_health(router: APIRouter, wiring: Wiring) -> None:
    get_health_handler = wiring.async_handlers['get_health']

    async def get_health() -> Response:
        return respond(await get_health_handler(), contract.GetHealth.RESPONSES)

    router.add_api_route(
        '/health',
        get_health,
        methods=['GET'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='getHealth',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'unscaffolded_basemodel',
                'operation': '/paths/~1health/get',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1health/get'),
    )


LITERAL_ROUTES: Final = (
    (contract.ListPets.OPERATION, _add_list_pets),
    (contract.GetHealth.OPERATION, _add_get_health),
)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    handlers: Mapping[str, Callable[..., object]] | None = None,
    authorizer: (
        Authorizer[SecretT, object]
        | AsyncAuthorizer[SecretT, object]
        | None
    ) = None,
    credential_extractors: CredentialExtractors[SecretT] | None = None,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Register the untagged operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        handlers=handlers,
        authorizer=authorizer,
        credential_extractors=credential_extractors,
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
        scaffold=contract.SCAFFOLD,
    )
