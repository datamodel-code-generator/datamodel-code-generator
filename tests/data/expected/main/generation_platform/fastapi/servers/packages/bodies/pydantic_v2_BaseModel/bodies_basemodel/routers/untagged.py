# Generated by datamodel-code-generator; target=fastapi; schema_version=1
"""Endpoints of the untagged operations; regenerate them instead of editing."""

from collections.abc import Callable, Mapping, Sequence
from typing import Annotated, Final

import bodies_basemodel_models
from fastapi import APIRouter, Depends, File, Form, Request, UploadFile
from fastapi.responses import Response

from .._generated import contract
from .._generated.contract import OperationDependencies
from .._runtime.model_codecs.unset import Unset
from .._runtime.model_codecs.values import DecodedValue
from .._runtime.server.application import Dependency, Wiring, build
from .._runtime.server.requests import absent, present
from .._runtime.server.responses import respond
from .._runtime.server.security import SecretT
from ..auth_types import AsyncAuthorizer, Authorizer, CredentialExtractors


def _add_post_item(router: APIRouter, wiring: Wiring) -> None:
    post_item_handler = wiring.handlers['post_item']

    def post_item(
        *,
        body: Annotated[bodies_basemodel_models.Item | Unset, Depends(contract.PostItem.BODY)],
    ) -> Response:
        return respond(post_item_handler(body=body), contract.PostItem.RESPONSES)

    router.add_api_route(
        '/items',
        post_item,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postItem',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'bodies_basemodel',
                'operation': '/paths/~1items/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1items/post'),
    )


def _add_post_account(router: APIRouter, wiring: Wiring) -> None:
    post_account_handler = wiring.handlers['post_account']

    def post_account(
        *,
        body: Annotated[DecodedValue[bodies_basemodel_models.Account], Depends(contract.PostAccount.BODY)],
    ) -> Response:
        return respond(post_account_handler(body=body), contract.PostAccount.RESPONSES)

    router.add_api_route(
        '/accounts',
        post_account,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postAccount',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'bodies_basemodel',
                'operation': '/paths/~1accounts/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1accounts/post'),
    )


def _add_post_profile(router: APIRouter, wiring: Wiring) -> None:
    post_profile_handler = wiring.handlers['post_profile']

    def post_profile(
        *,
        body: Annotated[tuple[str | None, bodies_basemodel_models.FieldProfilesPostRequest | bodies_basemodel_models.FieldProfilesPostRequest1 | Unset], Depends(contract.PostProfile.BODY.receive)],
    ) -> Response:
        return respond(
            post_profile_handler(body=body[1], media_type=body[0]),
            contract.PostProfile.RESPONSES,
        )

    router.add_api_route(
        '/profiles',
        post_profile,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postProfile',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'bodies_basemodel',
                'operation': '/paths/~1profiles/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1profiles/post'),
    )


def _add_put_document(router: APIRouter, wiring: Wiring) -> None:
    put_document_handler = wiring.handlers['put_document']

    def put_document(
        *,
        body: Annotated[tuple[str | None, bodies_basemodel_models.Item | bodies_basemodel_models.FieldDocumentsPutRequest], Depends(contract.PutDocument.BODY.receive)],
    ) -> Response:
        return respond(
            put_document_handler(body=body[1], media_type=body[0]),
            contract.PutDocument.RESPONSES,
        )

    router.add_api_route(
        '/documents',
        put_document,
        methods=['PUT'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='putDocument',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'bodies_basemodel',
                'operation': '/paths/~1documents/put',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1documents/put'),
    )


def _add_put_blob(router: APIRouter, wiring: Wiring) -> None:
    put_blob_handler = wiring.handlers['put_blob']

    def put_blob(
        *,
        body: Annotated[bytes, Depends(contract.PutBlob.BODY)],
    ) -> Response:
        return respond(put_blob_handler(body=body), contract.PutBlob.RESPONSES)

    router.add_api_route(
        '/blobs',
        put_blob,
        methods=['PUT'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='putBlob',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'bodies_basemodel',
                'operation': '/paths/~1blobs/put',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1blobs/put'),
    )


def _add_post_form(router: APIRouter, wiring: Wiring) -> None:
    post_form_handler = wiring.handlers['post_form']

    def post_form(
        *,
        body: Annotated[bodies_basemodel_models.FieldFormsPostRequest | Unset, Depends(contract.PostForm.BODY)],
    ) -> Response:
        return respond(post_form_handler(body=body), contract.PostForm.RESPONSES)

    router.add_api_route(
        '/forms',
        post_form,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postForm',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'bodies_basemodel',
                'operation': '/paths/~1forms/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1forms/post'),
    )


def _add_post_native_form(router: APIRouter, wiring: Wiring) -> None:
    post_native_form_handler = wiring.handlers['post_native_form']

    def post_native_form(
        *,
        name: Annotated[str, Form(alias='name')],
        count: Annotated[int, Form(alias='count')] = 1,
    ) -> Response:
        return respond(
            post_native_form_handler(name=name, count=count),
            contract.PostNativeForm.RESPONSES,
        )

    router.add_api_route(
        '/native-forms',
        post_native_form,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postNativeForm',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'bodies_basemodel',
                'operation': '/paths/~1native-forms/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1native-forms/post'),
    )


def _add_upload(router: APIRouter, wiring: Wiring) -> None:
    upload_handler = wiring.handlers['upload']

    def upload(
        *,
        file: Annotated[UploadFile, File(alias='file')],
        note: Annotated[str, Form(alias='note', default_factory=absent)],
    ) -> Response:
        return respond(
            upload_handler(file=file, note=present(note)),
            contract.Upload.RESPONSES,
        )

    router.add_api_route(
        '/uploads',
        upload,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='upload',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'bodies_basemodel',
                'operation': '/paths/~1uploads/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1uploads/post'),
    )


def _add_post_check(router: APIRouter, wiring: Wiring) -> None:
    post_check_handler = wiring.handlers['post_check']

    def post_check(
        *,
        body: Annotated[bodies_basemodel_models.Check | Unset, Depends(contract.PostCheck.BODY)],
    ) -> Response:
        return respond(post_check_handler(body=body), contract.PostCheck.RESPONSES)

    router.add_api_route(
        '/checks',
        post_check,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postCheck',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'bodies_basemodel',
                'operation': '/paths/~1checks/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1checks/post'),
    )


def _add_post_raw(router: APIRouter, wiring: Wiring) -> None:
    post_raw_handler = wiring.handlers['post_raw']

    def post_raw(*, request: Request) -> Response:
        return respond(post_raw_handler(request=request), contract.PostRaw.RESPONSES)

    router.add_api_route(
        '/raw',
        post_raw,
        methods=['POST'],
        status_code=204,
        response_model=None,
        response_class=Response,
        operation_id='postRaw',
        response_description='Done.',
        openapi_extra={
            'x-dcg-operation': {
                'version': 1,
                'package': 'bodies_basemodel',
                'operation': '/paths/~1raw/post',
            },
        },
        dependencies=wiring.dependencies.get('/paths/~1raw/post'),
    )


LITERAL_ROUTES: Final = (
    (contract.PostItem.OPERATION, _add_post_item),
    (contract.PostAccount.OPERATION, _add_post_account),
    (contract.PostProfile.OPERATION, _add_post_profile),
    (contract.PutDocument.OPERATION, _add_put_document),
    (contract.PutBlob.OPERATION, _add_put_blob),
    (contract.PostForm.OPERATION, _add_post_form),
    (contract.PostNativeForm.OPERATION, _add_post_native_form),
    (contract.Upload.OPERATION, _add_upload),
    (contract.PostCheck.OPERATION, _add_post_check),
    (contract.PostRaw.OPERATION, _add_post_raw),
)
TEMPLATED_ROUTES: Final = ()


def build_router(
    *,
    handlers: Mapping[str, Callable[..., object]] | None = None,
    authorizer: (
        Authorizer[SecretT, object]
        | AsyncAuthorizer[SecretT, object]
        | None
    ) = None,
    credential_extractors: CredentialExtractors[SecretT] | None = None,
    dependencies: Sequence[Dependency] = (),
    operation_dependencies: OperationDependencies | None = None,
    prefix: str = "",
) -> APIRouter:
    """Register the untagged operations on a new router, literal paths first."""
    return build(
        (*LITERAL_ROUTES, *TEMPLATED_ROUTES),
        contract.SCHEMES,
        handlers=handlers,
        authorizer=authorizer,
        credential_extractors=credential_extractors,
        dependencies=dependencies,
        operation_dependencies=operation_dependencies,
        prefix=prefix,
        scaffold=contract.SCAFFOLD,
    )
