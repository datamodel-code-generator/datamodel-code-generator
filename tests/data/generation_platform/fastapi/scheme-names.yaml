openapi: 3.1.0
info: {title: Scheme names, version: "1.0"}
paths:
  /keys:
    get:
      operationId: getKeys
      security:
        - api-key: []
        - api_key: []
      responses:
        "204": {description: Done.}
components:
  securitySchemes:
    api-key: {type: apiKey, in: header, name: X-Key}
    api_key: {type: apiKey, in: query, name: key}
