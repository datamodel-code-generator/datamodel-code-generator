openapi: 3.1.0
info: {title: Argument errors, version: "1.0"}
paths:
  /items:
    get:
      operationId: listItems
      parameters:
        - {name: req, in: query, schema: {type: string}}
        - {name: page, in: query, schema: {type: integer}}
        - {name: size, in: header, schema: {type: integer}}
      responses:
        "204": {description: Done.}
