"""Service interfaces of {{ context.info.title }}; regenerate them instead of editing."""

from __future__ import annotations

{{ imports }}
{%- if typevar %}

PrincipalT_contra = {{ typevar }}("PrincipalT_contra", contravariant=True)
{%- endif %}
{%- for protocol in protocols %}


class {{ protocol.name }}({{ protocol.base }}):
    """{{ protocol.docstring }}"""
{%- for method in protocol.methods %}

    @{{ abstract }}
    {{ method }}
{%- endfor %}
{%- endfor %}
