openapi: 3.1.0
info: {title: Codec errors, version: "1.0"}
paths:
  /choices:
    get:
      operationId: getChoices
      parameters:
        - {name: choice, in: query, schema: {anyOf: [{type: string}, {type: integer}]}}
      responses:
        "204": {description: Done.}
