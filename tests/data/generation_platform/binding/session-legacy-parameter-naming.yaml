openapi: 3.1.0
info:
  title: Legacy shared enum parameter
  version: "1"
paths:
  /pets:
    get:
      parameters:
        - $ref: '#/components/parameters/Status'
      responses:
        "204":
          description: Listed
  /toys:
    get:
      parameters:
        - $ref: '#/components/parameters/Status'
      responses:
        "204":
          description: Listed
components:
  parameters:
    Status:
      name: status
      in: query
      schema:
        type: string
        enum: [available, sold]
