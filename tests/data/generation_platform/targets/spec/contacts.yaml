openapi: 3.1.0
info:
  title: Contacts
  version: "1"
paths:
  /contacts:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Contact"
      responses:
        "201":
          description: The stored contact.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Contact"
components:
  schemas:
    Contact:
      type: object
      required: [id, mail]
      properties:
        id:
          type: string
          format: ulid
        mail:
          type: string
          format: email
        born:
          type: string
          format: date
